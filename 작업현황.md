# 작업현황 — 한국단열 단가표 (최종 업데이트: 2026-09-21)

## 2026-09-21: 부자재(hk_sub) 1단계 기준 판매가 구현 (Codex)

- `js/pricing-hankook-sub.js`에 승현기업 33개, 함일셀레나 12개, 투원테크 4개, 유니산업 4개, 형제산업 3개, 열선커터기 11개, 기타부자재 17개 등 부자재 84개를 상품코드별 평면 표로 추가했다. 기준 자료는 2026-06-09 단가표다.
- 표시 항목: 업체명, 제품명, 상품코드, 개당 원가, 이전 판매가, 편집 가능한 개당 판매가, 배송비, 마진·마진율, 수수료 6%, 부가세 10%, 개당 마진, 순수마진율, 참고마진율.
- 배송비는 결제 시 별도 청구하는 참고값이라 순수마진에서 차감하지 않는다. 계산식은 `판매가-원가-수수료-부가세`로 원본 표와 같다.
- 부자재 가격은 판매가를 직접 수정하지 않고 공급원가 셀에서 조정한다. `+100`, `-100` 같은 증감값 또는 새 원가를 입력하면 원가 변동분만큼 판매가도 자동으로 움직이며 마진을 다시 계산한다. 원가와 판매가는 함께 되돌릴 수 있고 DB 이력에도 같이 저장된다.
- `hkSubProductIndex()`를 `pricing-hankook-db.js` 상품 색인에 합쳐 기존 Supabase 저장·불러오기·이력에 상품코드별 판매가가 포함된다. 배송 및 몰별 상품 연결은 자료를 받은 뒤 추가한다.

## ⭐ 2026-09-21: 스티로폼(hk_bead) 1단계 구현됨 — 2단계 배송·채널 몰 상품은 아직 (Claude)

사용자가 1단계 엑셀을 4묶음(#1~#4)으로 나눠 줬고 전부 취합해서 구현했다. **커밋·푸시는 아직 안 함**(사용자가 요청할 때만).

- **구조**: 아이소핑크 표 엔진(`_hkIsoDraftSalesTable`, `recalcHkIsoDraftRow` 등)을 그대로 쓰고, 스티로폼 전용은 새 파일 `js/pricing-hankook-bead.js`(index.html에서 `pricing-hankook.js` 다음, db.js 앞)에 둔다 — 데이터(`HK_BEAD_ACCORDIONS`), 원가 카드, 상위 탭. 공용 엔진에서 고친 것: `_hkIsoDraftDivisor`에 1200*900(=900*1800 원장 반 잘라 **1.5분할**), `_hkIsoDraftSalesTable(…, draft)`의 옵션 인자(`line:'bead'`, `isAdhesive`, `deductShipping:false`, `hideUnifiedShipping`, `shippingHeader`), 행 원가 갱신을 `_hkIsoDraftApplyRowCost`로 분리, 아이소핑크 공통 원가 갱신(`updateHkIsoDraftMargin('shared')`)이 스티로폼 행(`data-line="bead"`)을 건드리지 않게 `:not([data-line="bead"])`.
- **화면**: 상위 탭 3개(1종 3호 17K / 접착식 1종 3호 / 2종 2호 25K) 안에 규격 그룹 아코디언 15개(고유 102행). 그룹 id: `bead1_600x900`(12행)·`bead1_600x900_bundle`(7)·`bead1_600x900_high_t`(6)·`bead1_900x1800`(7)·`bead1_900x1800_high_t`(6)·`bead1_900x1200`(6)·`bead1_1200x2400`(9) / `beadA_600x900`(6)·`beadA_900x1800`(3) / `bead2_600x900`(10)·`bead2_600x900_high_t`(6)·`bead2_900x1800`(3)·`bead2_900x1800_high_t`(6)·`bead2_900x1200`(6)·`bead2_1200x2400`(9). 상위 탭 전환은 `setHkBeadSuperTab`(아이소핑크와 클래스 분리: `.hk-bead-super-pane`).
- **원가 = 기본 원가 × 원장 면적 + 추가마진(사용자 제공 2026-09-21: 1종 3호 58원, 2종 2호 77원, 단위 원/㎡·mm)**: 카드에서 등급별 **기본 원가**(`HK_BEAD_BASE_COSTS` b1=58·b2=77)를 바로 고치고, 원장 규격별 **추가마진**(`HK_BEAD_UNIT_MARGINS`)은 아이소핑크처럼 **"마진 편집" 버튼 → 팝업(`#hkBeadMarginModal`, 확인을 눌러야 반영)**에서 고친다. 원장 원가 = `round(기본 원가 × 면적㎡)`(900*1800=1.62, 1200*2400=2.88; 58×1.62→94, 58×2.88→167, 77×1.62→125, 77×2.88→222), 적용 원가 = 원장 원가 + 추가마진 → `HK_BEAD_UNIT_COSTS`(`_hkBeadRecomputeUnits()`가 채움). **추가마진 기본값 21/28/25/23은 기존 적용 원가 115/195/150/245가 그대로 나오게 내가 역산한 값**이다(사용자 확인 필요 — 표 값은 안 바뀜). 접착식 원가(아래)도 이 적용 원가를 따른다. DB `hk_settings.bead_costs` = `{base:{b1,b2}, margins:{b1_900,…}}`(적용 원가는 저장하지 않고 다시 계산). 아래 "원가는 두께 구간… mm당 원가 4개" 설명은 이 구조의 결과값이다. 원가 = 적용 원가 × 두께(접착식은 + 접착 가공비 — **아이소핑크와 같은 `HK_ISO_DRAFT_ADHESIVE_ADDON` 공유**, 카드의 "접착 가공비" 버튼이 같은 팝업). 접착식은 1종 3호 900*1800 원가(`b1_900`)를 쓴다 — 접착식 900*1800 3행은 엑셀에 대흥정품 150원으로 적혀 있었으나 **사용자 지시로 115원 기준으로 바꿨고 판매가는 그대로**(그래서 이 3행의 판매원가·마진·순수마진율이 엑셀과 다름: 20T-7 마진 37,550→42,450 등). 메모 "1종 17K 58원/㎡=167원/mm, 2종 22K 77원/㎡=221원/mm"는 계산에 안 쓰고 카드에 참고로만 표시(1200*2400 원가 195·245는 이 값+28·+24).
- **결정 사항(사용자 확인 필요했던 것, 기본값으로 진행)**: ① 1종 3호 1200*2400은 엑셀에 두 버전(판매가 89,000원 계열 vs 90,000원 계열, 후자에 위 메모가 붙어 있고 참고마진이 전부 40%)이 와서 **후자(90,000원 계열)** 사용. ② 배송비 칸은 원본 엑셀처럼 **참고용 — 장당마진·예상판매가에서 빼지 않는다**(아이소핑크 600 고티는 뺌. 스티로폼은 그룹 옵션 `deductShipping:false`). 사용자가 이의 없이 진행.
- **상품코드(사용자 제공 규칙)**: `St_`(1종 3호) / `StA_`(접착식) / `Neo_`(2종 2호) + 판매크기(`*`→`_`) + 두께 + 수량 — 예 `St_430_430_50_2`, `StA_600_900_20_2`, `Neo_430_430_50_3`. 사용자가 준 9개 예시 코드가 전부 생성된 코드와 일치. 900*1200 판매는 `St_1200_900_100_2`처럼 판매사이즈 표기 그대로(`1200*900`) — **실제 등록 코드가 다르면 2단계/채널 자료 받을 때 맞출 것**. 코드는 행 데이터가 아니라 `_hkIsoDraftProductCode(saleSize, 두께, false, 접두어)`로 만든다.
- **2단계 배송이 없어서** 배송 정책·가격 조정·현재 판매가 열은 숨겼다(`hideUnifiedShipping`). 2단계 자료를 받으면 `HK_ISO_SHIPPING_BLOCKS`에 스티로폼 블록(`sourceAccordion: 'bead1_…'`)을 추가하고 `hideUnifiedShipping`을 끄면 된다(블록 없으면 fallback 코드 접두어는 `HK_ISO_CONNECTED_DRAFTS[id].codePrefix`).
- **행 데이터 생성 방식**: `js/pricing-hankook-bead.js`의 `HK_BEAD_ACCORDIONS[].rows`는 두께(`t`)·판매사이즈-수량(`s`)·판매가(`p`)·참고마진(`r`)·원본 원장판매가(`sp`)·엑셀 배송비(`sh`)만 갖고, 원가·판매원가·마진·수수료·장당마진은 `hkBeadRefreshDerived()`가 화면 그리기 직전에 계산해 채운다(원장 판매가가 원가와 같은 KS 표는 `f:1`로 원가를 따라감). 같은 두께는 붙어 있어야 한다(첫 행만 이름·원가 표시).
- **DB**: 판매가는 `hk_products`(코드별, `category_id:'hk_bead'`, `accordion_id`, `row_index`), 원가는 `hk_settings.bead_costs`(SQL 변경 없음). `_hkDbProductIndex`에 `hkBeadProductIndex()`가 합쳐지고 저장·불러오기·이력 복원·`hkDbSave`의 단가 기준 년월 동기화(`hkIsoBaseMonth`·`hkBeadBaseMonth` 두 입력칸)를 다 탄다. **사용자가 "단가표 저장"을 한 번 눌러야 스티로폼 102행이 DB에 올라간다**(안 눌러도 JS 기본값으로 보인다).
- **CSS 주의**: 아이소핑크 표 CSS가 참고마진·순수마진율 칸을 `nth-child(19)/(18)` 위치로 꾸미고 있어서, 열이 3개 없는 스티로폼 표에서 스타일이 빠졌다(사용자 지적) → `td.hk-iso-draft-ref-margin`·`td.hk-iso-draft-rate` 클래스 선택자로 바꿨다(아이소핑크와 계산된 스타일 동일 확인). 표에 열을 더하거나 빼면 nth-child 규칙(`3`·`6` 등)을 다시 확인할 것. **원가 카드 표 좌하단 라운드**: 공통 규칙(`.pricing-cost-unified-table`)에 우하단 라운드만 있어 아이소핑크·스티로폼 카드 표의 좌하단이 각져 있었다(사용자 지적) → `.hk-iso-base-cost-table tbody tr:last-child td:first-child`에 좌하단 8px를 추가했다(계산된 스타일로 확인). **원가 카드 통일(사용자 지시)**: 처음엔 스티로폼 카드에 기본 원가·원장 원가·추가마진·적용 원가를 다 늘어놓았는데 아이소핑크 카드(기본 원가만)와 달라 복잡하다는 지적 → 스티로폼 카드도 아이소핑크와 같은 클래스·모양(`pricing-cost-unified-table hk-iso-base-cost-table`, 등급 | 기본 원가 입력칸 2행)으로 줄였고, 추가마진·적용 원가는 "마진 편집" 팝업(등급 | 원장 규격+면적 | 기본 원가(원장 환산) | 추가마진 | 적용 원가)에서만 보인다. 스티로폼 전용 카드 CSS(`.hk-bead-cost-table` 등)는 지웠다.
- **검증**: 브라우저에서 102행 전부를 엑셀 값과 대조 — 99행(접착식 900*1800 3행 제외) 중 97행 완전 일치, 2행(`Neo_430_430_30_3`·`Neo_430_430_50_3`)은 마진 1원 차이(엑셀은 분할 원가 반올림값으로 뺀 듯, 엔진은 원본 소수로 계산 — 아이소핑크와 같은 방침). 원가 편집(해당 등급·접착식만 갱신, 아이소·타 등급 불변), 접착 가공비 편집, 판매가 편집(변경 전 표시), 가짜 Supabase로 저장→기본값 복구→불러오기 복원(원가·가공비·판매가·기준 년월)까지 확인. 실제 DB에는 쓰지 않았다.
- **다음**: 스티로폼 **2단계 배송** 엑셀 → `HK_ISO_SHIPPING_BLOCKS` 블록 → 그다음 **채널별 몰 상품**(한국단열·홈페이지·쿠팡·ESM·11번가, 한국단열라이프는 스티로폼 있음/없음 확인, 쿠팡_부자재는 부자재 때). 채널 쪽은 `_hkChannelTargetPrice`(categoryId `hk_bead` 분기)·`_hkIsoLookupFinalPriceByCode` 같은 조회 함수를 스티로폼 코드도 찾도록 확장해야 한다.

> **부자재(hk_sub) 카테고리는 사용자가 Codex로 작업할 예정이다(2026-09-21 사용자 말).** Codex가 이어받을 때 참고: 새 카테고리는 아이소핑크·스티로폼처럼 `renderHkCategoryPane`에 분기를 넣고, 표는 `_hkIsoDraftSalesTable`(옵션 인자 `draft`)·저장은 `pricing-hankook-db.js`의 상품 색인에 `window.hkXxxProductIndex()`를 합치는 방식이다 — 구현 패턴은 `js/pricing-hankook-bead.js`(스티로폼)가 가장 가깝고, 이 파일 맨 위 주석과 아래 스티로폼 절에 이유와 주의점(CSS nth-child 위치 규칙, 공통 원가 갱신이 다른 카테고리 행을 건드리지 않게 `data-line` 분리 등)이 적혀 있다. 쿠팡_부자재 채널은 이 카테고리 때 함께 다룬다. **부자재 화면 디자인 정리(2026-09-22, Claude — 사용자 요청 "Codex가 한 부자재 작업의 디자인만 아이소핑크·스티로폼에 맞게")**: 기능·데이터·저장 코드는 그대로 두고 화면만 맞췄다 — ① 위에 아이소핑크·스티로폼과 같은 모양의 "공통 설정" 카드(단가 기준 년월 입력칸 `hkSubBaseMonth`, 세 카테고리 입력칸이 `hkIsoSetBaseMonth`로 서로 동기화) ② 표를 **업체별 아코디언**(승현기업·함일셀레나·투원테크·유니산업·형제산업·열선커터기·기타부자재, 첫 업체만 열림, `상품 N개`, 오른쪽 화살표만 토글 — `_hkSubGroups`/`_hkSubAccordionHtml`)로 나눔(그래서 "업체명" 열은 뺐다; 행 번호 `data-row-index`는 `HK_SUB_PRODUCTS` 전체 기준 그대로, 저장 코드가 쓰는 카드 id `hkIsoAcc-sub_all`도 그대로 — 닫힌 아코디언 안의 행도 저장 때 정상으로 읽힘) ③ 카드 제목 "한국단열 부자재 기준 판매가" ④ 공급원가 직접 수정·조정액 입력을 한 줄로 합쳐 행 높이(90→67px)를 아이소핑크와 맞춤 ⑤ 칸 색: 마진·수수료·부가세·개당 마진은 색 없이, 순수마진율만 초록, 참고마진율만 빨강(아이소핑크와 같은 색·굵기·왼쪽 2px 테두리), "이전 판매가" 머리글의 연두색은 남색으로, 상품코드 칸은 아이소핑크 코드 칸 스타일(보라 모노스페이스, 가운데). 제품명은 첫 열이 되면서 `.pricing-table tbody td:nth-child(1)`의 가운데 정렬이 이겨서 `#page-pricing …` 선택자로 왼쪽 정렬을 지켰다. **가격 인상 이력(2026-09-22, 사용자 요청)**: 올해 중동 전쟁·나프타 수급 문제로 부자재 가격을 여러 번 올려서 "언제 얼마 올렸는지"를 상품별로 남긴다(엑셀의 "26.04.10 추가인상 …" 열과 같은 정보). 부자재 표에 **"가격 인상 이력" 열**(공급원가 오른쪽)이 있고 최근 3건을 `26.04.10 +250` 배지로(올림 빨강·내림 파랑, 4건 이상이면 "외 N건") 보여주며 아래에 누적 금액·횟수. **칸을 누르면 편집 팝업**(`#hkSubIncreaseModal`, 날짜·금액 수정·삭제·추가, 잘못된 입력은 막음). 항목 = `{date:'YYYY-MM-DD', amount}`(공급원가와 판매가를 같은 금액만큼 올림, 내리면 음수)이고 `product.increases`에 들어 있다. **공급원가 조정 [적용]·연필 직접 수정은 오늘 날짜로 자동 기록**된다(같은 날 저장 전 여러 번이면 합침, 합이 0이면 삭제, 저장 전에는 점선 배지 `pending`이고 "되돌리기"를 누르면 함께 사라짐). 시드 `HK_SUB_INCREASE_SEED`는 사용자가 준 엑셀 값 — **승현기업**(27개 상품·29건: 04-10 타이거폼 +250 / 이지본드·우레탄폼·스프레이폼·폼본드 +350 / 2K 세트 +9,000, 05-06 스프레이폼+251폼건 +350, 06-09 2K 세트 +16,000·노즐 +10,000·팁 +2,000, 08-21 스티커 제거제 +500)과 **나머지 업체**(22개 상품·22건: 함일셀레나 05-06 +300(폼크리너 2개는 +500), 투원테크 04-29 라이트폼 경질 +53,000·연질 +70,000·팁노즐 +5,000·건세트 +10,000, 유니산업 04-10 +500(패스트본드 3개), 형제산업 06-09 하이테크접착제 +2,250, 열선커터기 07-02 엘림 +1,000·엘림 열선 +100·USB +1,000) — + **기타부자재** 곰팡이제거제(MR) 05-11 +500 — 합계 **50개 상품·52건**(기타부자재 나머지 16개는 엑셀에 값 없음). 이력이 없는 업체/상품(월드251폼건·유니폼건·형제산업 나머지 2개·열선커터기 대부분·타이거폼건 계열·스프레이 접착제 등)은 엑셀에도 값이 없었다. 두 번째 엑셀(다른 업체)의 머리글은 26.07.02까지만 보였고 26.08.21 열은 잘려 있었다 — 그 열에 다른 업체 값이 더 있을 수 있으니 필요하면 사용자에게 확인하거나 화면에서 직접 입력. 엑셀의 날짜 열 중 26.05.11·06.23은 어느 행에도 값이 없었다. 저장은 `hk_products.shipping` JSON의 `increases`(SQL 변경 없음, 이력 스냅샷에도 포함; DB에 저장된 이력이 있으면 시드보다 우선, 빈 목록도 저장값으로 취급). 저장 성공 시 `hkSubMarkSaved()`(db.js가 호출)가 점선 표시를 지우고 "변경 전 공급원가"도 저장값 기준으로 맞춘다(이전엔 공급원가 쪽 기준이 저장 후에도 안 바뀌었다). **이 이력은 현재 공급원가·판매가를 바꾸지 않는 기록용**이다 — 표의 공급원가 값에 과거 인상분이 이미 들어 있는지는 사용자에게 아직 확인하지 않았다(엑셀 제목은 "2026.06.09 기준"인데 이력 열에는 06.23·07.02·08.21도 있다).

## ✅ 2026-09-22: 스티로폼 2단계(배송) **구현 완료(미커밋)** — 사용자 결정 6개 모두 반영 (Claude)

**구현 내용**: `js/pricing-hankook-bead.js`에 `HK_BEAD_SHIPPING_BLOCKS`(코드 → 배송값 사전 13개 블록)를 두고, 1단계 행 순서에 맞춰 `rows`를 만들어 `HK_ISO_SHIPPING_BLOCKS`에 등록한다(`categoryId:'hk_bead'`). 1단계 표에는 배송 정책·가격 조정·현재 판매가 열이 다시 나온다(블록이 없는 `bead1_1200x2400`·`bead2_1200x2400`만 계속 숨김 — 이 두 그룹은 2단계 자료를 안 받았다). 스키마: per5(1종·접착식 기준 6,000 / 2종 5,500, `sharedBaseShipping`이라 "판매 시 5장당 배송비"를 블록별로 고칠 수 있음), per1Coupon 무료배송(고티·900*1200), **새 `free90`**(무료배송 + 10% 네고 — `_hkIsoFree90Parts`: 플러스 = 판매가+실제배송비, ×0.9, 100원 미만이 단수정리, 실판매가 = 100원 단위 버림; 엑셀 13행 전부 일치 확인). `_hkIsoBlockRowFinalPrice`가 schema별 실판매가를 하나로 모았고(`_hkIsoLookupFinalPriceByCode`가 사용), 배송 세부설정 팝업은 카테고리별로 블록을 걸러 보여준다(`window._hkIsoShippingCategory`, 스티로폼 카드에 "배송 세부설정" 버튼, 표의 "배송 정책" 칸을 눌러도 해당 카테고리로 열림; 팝업 제목 `hkIsoShippingModalTitle`).
**그룹 이름(사용자 결정 2026-09-22)**: 스티로폼 600x900의 "일반"→**"기본 옵션"**(상품번호 437331834·3505478787 스마트스토어 옵션, 1종·2종 둘 다), "묶음"→**"묶음 옵션"**(부제 "쿠팡전용 포함", 상품번호 446014684·쿠팡전용), "후판"→**"고티"**(아이소핑크와 같은 용어; 600x900 고티·900x1800 고티). 화면 이름(`label`)·배송 블록 제목(`title`)만 바꿨고 그룹 id는 그대로다(`*_high_t` 등). 900x1800 "일반"(10~100T 묶음 포함)은 이름을 그대로 뒀다.
**코드 별칭(사용자 결정)**: 900*1800은 `900_1800`으로 통일. `St_1800_900_…`/`Neo_1800_900_…`(상품번호 2913417918) 12개와 `SSt_600_900_100_1`은 `HK_BEAD_CODE_ALIASES`로 기준 코드에 매핑되고 `hkBeadNormalizeCode()`(정규식으로 미래의 `1800_900` 코드도 자동 매핑)가 `_hkIsoLookupFinalPriceByCode`·`_hkIsoBlockShippingByCode`에서 쓰인다. 배송 팝업의 코드 칸에는 `= 별칭`이 작게 함께 보인다. **채널별 몰 상품 자료를 받을 때 그 코드들을 기준 코드로 바꿔 넣을 것("나중에 줄 때 알아서 매핑" — 사용자 지시).**
**저장**: 스티로폼 블록은 `_hkDbProductIndex`의 블록 루프에서 빼고(`categoryId==='hk_bead'`이면 건너뜀 — 안 그러면 코드 중복 오류) `hkBeadProductIndex()`가 1단계 행과 같은 상품코드로 `block`·`ship`을 함께 내놓는다 → `hk_products.shipping`에 배송값, `hk_settings.block_base_shipping`에 per5 블록의 5장당 기준. 가짜 DB로 저장→기본값으로 되돌림→불러오기 복원(free90 실제배송비·per5 플러스 금액) 확인, 상품 270개(아이소 83+스티로폼 103+부자재 84) 중복 없음.
**사용자 답변 반영(2026-09-22)**: 1단계에 `St_430_430_10_5`(1,500원) 추가, `StA_600_900_20_5`는 20,200원, `St_1200_900_100_2`는 1단계 39,000원 기준(2단계 엑셀의 34,000은 무시 → 실판매가 53,500), `St_900_1800_40_5`는 1단계 40T-5장에 연결(free90, 실판매가 50,800).
**⚠ DB 덮어쓰기 주의**: 저장된 값이 코드의 시드보다 **항상 우선**한다. 사용자가 스티로폼 102행을 이미 DB에 저장(2026-09, 실제 적용가)해서 **`StA_600_900_20_5`는 DB에 11,500원이 남아 코드의 20,200원이 안 보인다** — 화면에서 그 행 판매가를 20,200으로 고치고 "단가표 저장"을 눌러야 한다(사용자에게 알림). 앞으로 코드의 시드 값을 고쳐도 이미 저장된 행에는 반영되지 않는다. 새로 추가한 행(`St_430_430_10_5`)과 2단계 배송값은 DB에 없어서 시드가 그대로 보이고, 저장하면 올라간다. `Neo_600_900_20_3`(20T 600*900-3)은 2단계 엑셀에 없어 기준 배송비 그대로(차액 0·플러스 0, `assumed:true`)로 뒀다.
**1장당배송비차액**: 엑셀 1종 블록은 차액 그대로(÷5 안 함), 2종 블록은 ÷5였는데 화면은 아이소핑크 방식(÷5)으로 통일해서 1종 값이 엑셀과 다르게 보인다(참고용 칸, 실판매가에는 안 쓰임).
**한국단열 채널 스티로폼 상품 입력 완료(2026-09-22, 미커밋)**: 상품번호 10개·옵션 118행(`HK_CHANNEL_LISTINGS.hkd`, `js/pricing-hankook-bead.js` 맨 아래 `addBeadHkdChannelProducts` — 코드 목록만 두고 현재 판매가는 `_hkChannelTargetPrice`가 2단계 실판매가를 코드로 조회). 437331834(15)·446014684(7)·2216673728(7)·2913417918(36, 무료배송·`1800_900` 코드 포함)·3505478787(15)·3950515541(3)·3950655401(8, **접착식 스티로폼 StA_ 3 + 접착식 아이소핑크 IsoA_ 5가 한 상품에 섞임** — 카테고리는 상품 단위라 `hk_bead`로 두고 코드로 조회해서 IsoA_도 아이소핑크 값으로 계산)·5759250443(12, 무료배송+10% 네고)·5763066244(12)·7875494911(3). 상품 단위 값(배송비 6500/6000/0, 5개마다/1개마다/-, 제주 10000/30000/35000/90000, 편도교환)은 엑셀 그대로. **수정 전 판매가·배송비는 사용자 규칙대로 엑셀의 예전 값이 아니라 현재 값**(차액 0). 상품코드는 스토어 등록 코드 그대로 두고(`St_1800_900_…`·`SSt_…` 포함) 가격 조회만 별칭으로 기준 코드에 매핑. 옵션 상품명은 코드로 만든다(`hkBeadProductNameFromCode` — St 백색스티로폼 / StA 접착식_백색스티로폼 / Neo 회색스티로폼, 엑셀 이름과 동일). 채널 가격 조회를 `_hkChannelHkdPrice`로 묶어 `hk_bead`도 아이소핑크와 같이 찾는다(ESM·쿠팡 계산도 이 함수를 쓴다). 118행 전부 조회됨(null 0) — 엑셀 "현재 판매가"와 대조해 **117행 일치, 1행 차이: `St_1200_900_100_2`(엑셀 48,500 = 옛 34,000 기준 / 지금 53,500 = 사용자가 확인한 39,000 기준)** → 스토어에 실제 등록된 이 옵션 가격은 아직 옛 값이니 사용자가 직접 고쳐야 하는데, 수정 전 판매가를 현재 값에 맞추는 규칙 때문에 화면의 "반영 대기"는 0으로 나온다(사용자에게 알림). **3950515541과 3950655401은 번호가 비슷하지만 사용자가 오타가 아니라 서로 다른 상품이라고 확인함(2026-09-22)** — 그대로 두 상품으로 입력. 한국단열 채널 스티로폼 화면(10상품·118옵션)도 사용자가 이상 없다고 확인함. 가짜 DB로 저장(스티로폼 10상품·118옵션)→복원 확인.
**남은 것**: 스티로폼 **나머지 채널 몰 상품**(홈페이지·쿠팡·ESM·11번가 — 엑셀 아직 안 받음, 한국단열라이프에 스티로폼이 있는지도 확인), `_hkChannelTargetPrice`에 `hk_bead` 분기(스티로폼 코드로 2단계 실판매가 조회 — `_hkIsoLookupFinalPriceByCode`는 이미 스티로폼 코드를 찾으니 카테고리 조건만 넓히면 됨), 쿠팡 채널의 `_hkIsoBlockShippingByCode`는 per5 블록만 알아서 free/free90 블록의 배송비는 채널 규칙과 함께 정할 것.

### 아래는 2026-09-21에 정리한 2단계 원본 취합 결과(참고용 — 구현에 반영됨)

사용자가 "끝"이라고 했고 사용량이 부족해 여기서 멈췄다. 자료는 5묶음(#2-1~#2-5), **119행**을 받았다. (이 절 이하는 원본 취합 기록이다.) 판매가는 아래 예외 외에 전부 1단계와 일치한다(판매가는 1단계 행에서 가져오면 되고 2단계에는 배송값만 새로 필요하다).

### 블록 목록 (상품번호 / 그룹 / 행 / 구조)
구조 3종: **per5**(1종 기준 5장당 6,000 · 2종 기준 5,500) · **무료배송**(실판매가 = 판매가 + 플러스, 플러스 = 실제배송비) · **90%할인**(쿠팡전용 스타일).

| # | 상품번호 | 내용 | 행 | 구조 |
|---|---|---|---|---|
| A | 437331834 | 1종 3호 600*900 일반 12 + StA 3 | 15 | per5 |
| B | 3505478787 | A와 같음, 10T 430*430만 `St_430_430_10_5`(1,500) — 3행짜리 중복 블록(30T St 2 + StA 1)도 한 번 더 옴 | 15(+3중복) | per5 |
| C | 3950515541 | StA_600_900_20_2 / 30_1 / 50_1 | 3 | per5 |
| D | 쿠팡전용 | St_600_900_10_10(7,000) / 20_5(8,400) / 30_3(7,000) / 40_2(6,800) | 4 | per5 |
| E | 쿠팡전용 | `St_900_1800_40_5` 42,500 (판매사이즈 칸엔 "900*1800-3" 오타) | 1 | 90%할인 |
| F | 446014684 | St_430_430_50_3 4,500 · St_600_900_50_2 8,800 · St_430_430_100_3 12,000 · **`SSt_600_900_100_1`**(오타 코드) 8,200 · StA_600_900_20_2 4,500 / 30_1 5,500 / 50_2 15,000 | 7 | per5 |
| G | 쿠팡전용(접착식) | `StA_600_900_20_5` **20,200**(1단계는 11,500) · `StA_600_900_30_3` 18,000 | 2 | per5 |
| H | 2216673728 | 2종 2호: Neo_430_430_20_3 4,500 · Neo_430_430_30_3 7,600 · Neo_600_900_30_2 9,500 · Neo_430_430_50_3 11,500 · Neo_600_900_50_1 8,200 · Neo_430_430_100_2 15,500 · Neo_600_900_100_1 16,400 | 7 | per5(2종) |
| I | 쿠팡전용(2종) | Neo_600_900_30_3 13,500 · Neo_600_900_50_2 18,500 | 2 | per5(2종) |
| J | 2913417918 | 1종 600*900 고티 `St_600_900_{100_2,200_1,300_1,400_1,500_1,600_1}` 13,000/13,000/20,500/27,500/34,500/40,500 | 6 | 무료배송 |
| K | 2913417918 | 2종 600*900 고티 `Neo_600_900_…` 24,500/24,500/34,000/51,000/63,000/69,000 | 6 | 무료배송 |
| L | 2913417918 | 1종 1200*900 `St_1200_900_{100_2,200_1,…600_1}` 34,000(**1단계 39,000**)/39,000/59,000/79,000/99,000/119,000 | 6 | 무료배송 |
| M | 2913417918 | 2종 900*1200 `Neo_1200_900_…` 59,000/59,000/89,000/109,000/139,000/169,000 (일부 행 이름이 "2종3호" 오타) | 6 | 무료배송 |
| N | 2913417918 | 1종 900*1800 고티 **`St_1800_900_…`** 54,500/54,500/74,500/89,600/104,700/135,400 | 6 | 무료배송 |
| O | 2913417918 | 2종 900*1800 고티 **`Neo_1800_900_…`** 74,900/74,900/109,500/139,600/174,700/210,400 | 6 | 무료배송 |
| P | 5759250443 | St_900_1800_{10_10,20_7,30_5,40_3,50_3,100_1}(6) + Neo_900_1800_{30_5,50_3,100_1}(3) + StA_900_1800_{20_7,30_5,50_3}(3) | 12 | 90%할인 |
| Q | 5763066244 | 1종 900*1800 고티 **`St_900_1800_…`** (N과 값 같음) | 6 | 무료배송(플러스가 "단수정리" 칸에 들어 있음) |
| R | 5763066244 | 2종 900*1800 고티 **`Neo_900_1800_…`** (O와 값 같음) | 6 | 무료배송(위와 같이 칸 어긋남) |

### 배송 값 (상품코드 규칙 예외만)
- **per5(1종)**: 기준 6,000, 5장당 실제배송비 5,500(**10T 430*430만 5,200**), 배송비차액 = 실제 − 6,000(−500 / 10T 430은 −800), 1장당배송비차액 = **차액 그대로**(÷5 안 함), 플러스 금액 빈 칸 → 실판매가 = 판매가.
- **per5(2종, H·I)**: 기준 **5,500**, 1장당차액 = 차액 ÷ 5. 값 — 20_3: 실제 5,500(차액 0) / 30_3(430): 6,500·+1,000·200·**플러스 200 → 7,800** / 600_30_2: 5,500 / 50_3(430): 9,300·+3,800·760·**플러스 800 → 12,300** / 600_50_1: 6,500·+1,000·200(플러스 빈 칸 → 8,200) / 100_2(430)·600_100_1: 5,200·−300·−60 / I: 30_3 5,500(0) · 50_2 6,500·+1,000·200·**플러스 200 → 18,700**.
- **무료배송 고티(J·K·Q·R)**: 실제배송비 = 플러스 = 100~600T 순서로 **7,500/7,500/11,000/14,000/17,000/21,000**(600*900), **900*1800(N·O·Q·R)은 21,500/21,500/31,500/41,400/51,300/60,600**, **1200*900(L·M)은 14,500/14,500/21,000/28,000/34,400/40,800**. 실판매가 = 판매가 + 플러스(예: 13,000+7,500=20,500, 54,500+21,500=76,000).
- **90%할인(E·P)**: 배송비 플러스 금액 = 판매가 + 실제배송비, 원장판매할인금액 90% = 플러스 × 0.9, **단수정리 = 그 값의 100원 미만(= 100원 단위 버림용, 노란 입력칸)**, 실판매가(10% 네고) = 90% 금액 − 단수정리. P의 실제배송비·단수정리: St 10_10 14,000·50(→35,500) / 20_7 15,700·90(→43,200) / 30_5 16,900·50(→44,500) / 40_3 14,000·30(→34,800) / 50_3 16,900·60(→45,300) / 100_1 12,200·30(→31,200) / Neo 30_5 16,900·70(→69,500) / Neo 50_3 16,900·10(→66,500) / Neo 100_1 12,200·10(→42,200) / StA 20_7 21,500·50(→80,500) / StA 30_5 16,900·10(→69,200) / StA 50_3 16,900·10(→63,800). E: 42,500+14,000=56,500 → 50,850 → 50 → 50,800. (이 "단수정리 = mod 100" 관계는 눈으로만 확인했다 — 구현 때 코드로 검증할 것.)

### ✅ 사용자 답변(2026-09-22) — 1~5 결정됨, 6은 사용자가 더 자세한 설명을 요청해 대기
1. `SSt_600_900_100_1`: 사용자가 **일부러 그렇게 한 것 같다며 그대로 두라고 함** → 2단계 블록에서 `codeOverride: 'SSt_600_900_100_1'`(아이소핑크 `IIso_`와 같은 방식).
2. `StA_600_900_20_5`: **20,200원이 맞다** → 1단계 `beadA_600x900` 20T 600*900-5를 20,200원으로 고쳤다(2026-09-22 반영, 커밋 전이면 `git status` 확인; 원장판매가 참고값은 ×3=60,600, 참고마진은 54% 순수마진율에 맞춰 55%로 — 사용자에게 알려줄 것).
3. `St_1200_900_100_2`: **39,000원이 맞다** → 1단계 값(39,000)이 맞고 2단계 엑셀의 34,000이 틀린 것. 2단계 블록 L은 1단계 판매가를 가져오면 되므로 34,000은 무시.
4. `St_430_430_10_5`(1,500원): **1단계에 추가함**(`bead1_600x900` 10T 430*430-5, 참고마진은 같은 두께 430 행과 같은 45%로 가정 — 알려줄 것). 1단계 행이 103행이 됨.
5. `St_900_1800_40_5`: **판매사이즈 "-3"은 오타가 맞다** → 코드대로 1단계 40T 900*1800-5(`bead1_900x1800`, 42,500원)에 연결.
6. (대기) 900*1800 고티 이중 등록 코드 — 아래 6번 참고.

### (이전) 결정 필요 질문 원문 — 위 답변 반영 전
1. **`SSt_600_900_100_1`**(F, 100T 600*900-1 8,200): 아이소핑크 `IIso_`처럼 실제 등록된 오타 코드라면 `codeOverride`로 그대로 둔다(기본 제안). 정상 코드 `St_600_900_100_1`이 A·B에도 있어서 같은 행에 코드 둘이 붙는다.
2. **`StA_600_900_20_5`**(G): 2단계 20,200 vs 1단계 11,500 — 어느 쪽이 맞나(1단계 값이 틀렸으면 1단계 수정).
3. **`St_1200_900_100_2`**(L): 34,000 vs 1단계 39,000.
4. **`St_430_430_10_5`**(B, 1,500원): 1단계에 없는 행 — 1단계 `bead1_600x900`에 10T 430*430-5를 추가할지.
5. **`St_900_1800_40_5`**(E): 판매사이즈 "-3"은 오타로 보이고 42,500 = 1단계 40T-5장 가격 — 그렇다면 코드 `40_5`대로 1단계 40T 900*1800-5(`bead1_900x1800`)에 연결.
6. **이중 등록 코드**: N·O는 `St_1800_900_…`/`Neo_1800_900_…`, Q·R·P는 `St_900_1800_…`/`Neo_900_1800_…` — 같은 1단계 행(`bead1_900x1800_high_t`·`bead2_900x1800_high_t`)에 코드가 둘 붙는다. 아이소핑크의 `codeOverride` 방식처럼 블록별로 코드를 지정하고 상품코드 유일성은 블록 단위로 보되, 1단계 판매가 조회(`_hkIsoLookupFinalPriceByCode` 계열)가 두 코드 다 찾도록 한다. 900*1200도 표기가 `1200*900`(코드 `St_1200_900_…`) 하나뿐이라 문제 없음.

### 구현 메모
- 블록은 `HK_ISO_SHIPPING_BLOCKS`에 추가(`sourceAccordion: 'bead1_600x900'` 등, rows는 1단계 행과 **같은 순서**로 1:1). per5/무료배송(`per1Coupon`, `isFreeShipping:true`) 스키마는 이미 있다. **90%할인 스키마는 새로 만들어야** 한다(실제배송비 → 플러스 = 판매가+배송비 → ×0.9 → 단수정리 → 실판매가). 여러 상품번호가 같은 그룹을 공유하는 건 아이소핑크 블록처럼 `productNumbers` 배열로.
- 1단계 표에서 `hideUnifiedShipping`을 끄고 `HK_ISO_SHIPPING_BLOCKS`의 `sourceAccordion`으로 연결하면 배송 정책·가격 조정·현재 판매가 열이 나온다. 블록이 없는 그룹(예: 아직 배송값 없는 그룹)은 계속 숨기려면 그룹별 `hideUnifiedShipping`을 유지.
- 2단계가 끝나면 **채널별 몰 상품**(한국단열·홈페이지·쿠팡·ESM·11번가) 엑셀을 사용자가 준다. 한국단열라이프에 스티로폼이 있는지 확인, 쿠팡_부자재는 부자재 때.

## ⚠️ 2026-09-21: Supabase 저장 구현됨 — **SQL 실행 전에는 저장이 안 된다** (Claude)

새 파일: `sql/hankook_pricing_tables.sql`, `js/pricing-hankook-db.js`(index.html에서 `pricing-hankook.js` 다음에 로드). 기존 단가표처럼 RLS 끔(로그인 무관하게 anon 키로 읽기/쓰기).

**해야 할 일(사용자)**: Supabase 대시보드 > SQL Editor에서 `sql/hankook_pricing_tables.sql` 전체를 한 번 실행 → 한국단열 화면 우측 위 **"저장"** 버튼을 처음 한 번 눌러 현재 값을 DB에 올린다(별도 시드 없음). 테이블이 없으면 상태 표시줄에 안내 문구가 빨갛게 뜬다.

- 테이블 5개: `hk_settings`(원가구간·추가마진·접착 가공비·공통 5장당 배송비), `hk_products`(상품코드 → 1단계 판매가 + 배송 세부설정 jsonb), `hk_channel_products`/`hk_channel_items`(채널별 몰 상품·옵션, 옵션은 순번이 키), `hk_history`(저장할 때마다 전체 스냅샷, 최근 30개만 유지).
- **저장하는 것은 사람이 입력한 값만**이다. 실판매가·옵션추가금·가격차이는 화면에서 다시 계산한다.
- **구조는 JS, 값은 DB**: 어떤 상품/옵션이 있는지는 계속 `pricing-hankook.js` 상수가 정하고, DB는 그 위에 값(판매가·배송 정책·수정 전 판매가·재고 등)만 얹는다(불러오기 = overlay). 그래서 JS에 새 상품을 추가하면 저장 전에도 기본값으로 나타난다. 옵션은 (상품ID, 순번, 상품코드)가 모두 일치할 때만 DB값을 적용한다.
- **저장 = 현재 화면 전체로 DB를 맞춤**: 화면(JS)에 더는 없는 행은 DB에서도 지운다. 지웠던 값은 `hk_history` 스냅샷에서 복구 가능.
- 처음 한국단열로 진입할 때 한 번 자동으로 불러온다. 수정하면 상태 표시줄에 "저장 안 된 변경"이 뜨고, 그 상태로 새로고침·닫기 하면 브라우저가 경고한다. 위쪽 "저장 이력 + 불러오기"는 선택한 스냅샷을 화면에만 올리고(저장을 눌러야 DB 반영).
- 저장하면 각 판매가 셀의 "변경 전 금액" 표시가 방금 저장한 값 기준으로 초기화된다.
- **단가 기준 년월(2026-09-21 추가)**: "공통 원가·배송 설정" 카드(버튼 줄: 단가 기준 년월 / 마진 편집 / 접착 가공비 / 배송 세부설정 — 배송 세부설정은 예전 기준 판매가 카드 제목줄에서 2026-09-21에 옮겼다. 기준 판매가 표의 "배송 정책" 칸을 눌러도 같은 팝업이 열리고 그 행으로 이동해 잠깐 강조된다 — `openHkIsoShippingFor`)에 에너가드처럼 `단가 기준 년월`(month 입력)이 있다. 이 값이 `hk_history.label`이고(YYYY-MM), 같은 년월로 저장하면 그 년월 이력을 덮어쓰고 다른 년월이면 새 이력이 생긴다(이미 있는 다른 년월로 바꿔 저장하려 하면 덮어쓰기 확인창). 년월이 비어 있으면 이번 달로 저장한다. 이력 목록에서 저장된 년월을 고르면 바로 화면에 불러온다(저장해야 현재값이 됨). 기준 년월은 `hk_settings.base_month`에도 저장돼 처음 열 때 마지막 저장분의 년월이 보인다. 스키마 변경 없음(SQL 재실행 불필요). 이력은 최근 60개 유지. (예전 첫 저장이 시각 이름 `2026-09-21 12:40`으로 남아 `2026-09`와 중복이라, 내용이 같은 걸 확인하고 삭제함.)
- **헤드 우측 버튼(2026-09-21)**: 저장 관련 컨트롤은 한국단열 카드 헤드(회사 드롭박스 오른쪽, 에너가드와 같은 자리)의 `#hkPricingHeaderRight`로 옮겼다(예전의 탭 줄 아래 표시줄·저장 이력 select는 삭제). 구성: `최근 저장`(오류·"저장 안 된 변경"도 이 자리에 표시) / `실제 적용가` 배지 / `이력` 드롭다운(최신·조회중 배지, 항목별 "실제 적용" 버튼) / `단가표 저장`. `js/pricing.js`의 `setPricingCompany`가 한국단열일 때만 이 헤드를 보여준다.
- **실제 적용가**: 이력 한 개를 골라 "실제 적용"을 누르면 `hk_settings.live_month`에 그 년월이 저장된다(스키마 변경 없음). 지금 화면의 년월과 다르면 배지가 경고색. 실제 적용 중인 년월을 새 값으로 덮어써 저장하면 지정이 자동 해제된다(에너가드와 같은 규칙). **한국단열에는 아직 이 값을 읽어 쓰는 곳이 없다** — 어느 단가가 실제 반영분인지 표시하는 용도이며, 나중에 스토어 반영/견적서 연동을 붙일 때 `live_month` → `hk_history.snapshot`을 읽으면 된다.
- **수정 전 판매가·배송비 = "마지막으로 저장한 값"(2026-09-21 최종 규칙)**: `적용 완료` 버튼은 **삭제**했다(사용자 지시). 대신 **`단가표 저장`을 누를 때 모든 채널의 수정 전 판매가·배송비가 그 시점의 현재 판매가·배송비로 자동으로 맞춰진다**(`hkChannelAlignBaselines`, `hkDbSave`가 호출 — 저장이 실패하면 되돌림). 그래서 차액(붉은색)·`반영 대기 N`은 **마지막 저장 이후에 고친 것만** 보여주고, 저장하면 0으로 돌아간다. 저장 전에 스토어에 반영해야 할 변경을 확인해 두라는 뜻. 새 채널 엑셀을 받아 넣을 때도 수정 전 값은 현재 값으로 넣는다(아래 이력의 규칙 그대로). ↓ 아래는 이 규칙이 생기기까지의 기록이다(적용 완료 버튼 설명은 옛 것).
- **(이력) 수정 전 판매가·배송비 = "스마트스토어에 마지막으로 반영한 값"(2026-09-21 정리)**: 몰별 화면의 `수정 전 판매가`/`수정 전 배송비`는 엑셀에서 옮겨 둔 예전 스토어 값이었고 92옵션 전부 현재 판매가와 달랐다(차액 전부 ≠ 0, 442086644만 수정 전 배송비 6,500 vs 6,000). 사용자 요청으로 **한국단열 채널 92옵션의 수정 전 판매가·배송비를 현재 값으로 바꿔 DB에 저장했다**(차액 전부 0). 이후 1단계 단가를 고치면 그 옵션만 `차액`(주황 강조)이 생기고 채널 헤드에 `반영 대기 N`이 뜬다. 스토어에 실제 반영한 뒤 채널 헤드의 **`적용 완료`** 버튼(`hkChannelMarkApplied`)을 누르면 수정 전 값이 다시 현재 값으로 바뀌고(지금 보는 제품 종류 필터 범위만) `단가표 저장`을 눌러야 DB에 남는다. 예전 값 백업: 세션 scratchpad의 `backup_hk_channel_items_2026-09-21.json`(세션이 지워지면 사라짐). JS 시드(`HK_CHANNEL_LISTINGS`)의 prevPrice는 예전 엑셀 값 그대로라, DB를 비우면 예전 값으로 돌아간다.
- **기준가 옵션 선택·판매상태(2026-09-21)**: 몰별 화면에서 (1) 옵션이 둘 이상인 상품은 현재 판매가 칸의 라디오로 **기준가 옵션을 직접 지정**한다(파란 칸 = 기준가 옵션, 옵션추가금은 그 옵션과의 차이로 다시 계산). 지정 안 하면 "판매중인 첫 옵션"이 기준(전부 품절이면 첫 옵션). (2) 옵션마다 **판매상태 select(판매중/품절/판매중지)** — 품절·판매중지 옵션은 행이 흐려지고(품절 주황/중지 회색, 상품명 취소선) **반영 대기 대상에서 빠진다**(저장할 때 수정 전 값은 상태와 상관없이 현재 값으로 맞춰진다). 지정한 기준 옵션이 품절·중지되면 그 칸에 주황 점선 경고(옵션 하나뿐인 상품은 경고 없음). 채널 헤드에 `품절 N`/`판매중지 N` 배지. 저장은 **`hk_settings.channel_options` 한 줄(JSON)** — 기본값에서 벗어난 것만 `{채널:{상품ID:{base:코드, status:{코드:상태}}}}`로 담아서 **SQL 변경 없음**, 이력 스냅샷에도 포함. 상태·기준 옵션은 상품코드로 매칭한다(같은 상품 안에서 코드가 중복되면 같이 바뀜 — 현재 중복 없음). **재고수량은 판매상태를 따라간다(2026-09-21)**: 품절·판매중지는 재고수량 칸이 0(붉은 글씨, 툴팁으로 이유 표시), 판매중이면 원래 재고(`item.stock`, 없으면 한국단열/홈페이지 표 99,999,999·쿠팡 표 99,999)로 돌아온다 — `item.stock` 값 자체는 바꾸지 않고 표시만 계산하므로(`_hkChannelStock`, `HK_CHANNEL_STATUS_STOCK`) 판매중으로 되돌리면 저장된 재고가 그대로 나온다. 재고수량 열이 있는 표(한국단열·홈페이지·쿠팡)에만 적용되고 ESM·11번가 표에는 재고 열이 없다.
- **탭바 구분(2026-09-21)**: 카테고리 탭과 채널 탭을 `|` 하나로 나눠 놓던 걸 두 줄로 분리했다 — 윗줄 "제품별 단가(카테고리)" 인디고, 아랫줄 "몰별 적용(판매 채널)" 청록 배경·청록 선택색, 줄마다 이름표. 탭 id(`hkCategoryTabs`/`hkChannelTabs`)와 "두 줄에서 동시에 선택 안 됨" 규칙은 그대로.
- **접착 가공비(원장당)**: 카드의 입력칸을 없애고 `마진 편집` 옆 `접착 가공비` 버튼(팝업)으로 옮겼다. 값 자체와 저장(`hk_settings.adhesive_fee`)은 그대로.
- **한계**: 저장은 여러 요청으로 나뉘어 나가 한 트랜잭션이 아니다. 중간에 실패하면 일부만 저장될 수 있으니 실패 메시지가 뜨면 다시 저장한다. 채널 상품 데이터를 화면에서 추가·삭제하는 UI는 없다(값 수정은 JS 상수 → 저장).
- 검증: 실제 Supabase에는 아직 테이블이 없어 "테이블 없음" 경로만 실서버로 확인했고, 저장/불러오기/이력 복원/오래된 행 삭제/탭 상태 보존은 메모리 가짜 클라이언트로 확인했다(저장 후 새로고침·불러오기 결과가 저장 시점 화면과 259행 중 257행 동일, 나머지 2행은 이후 수정한 "변경 전" 문구 버그로 고침). **SQL 실행 후 실서버로 한 번 더 저장→새로고침 확인이 필요하다.**
- 상태 표시줄 위치(탭 줄 아래 오른쪽)는 화면으로 직접 보지 못하고 좌표만 확인했다.

## 2026-09-21: 기준가격·배송 통합 및 몰별 전상품 구조 (Codex)

- 리팩터링 전 상태는 로컬 체크포인트 커밋 `39fe4ba`에 보존했다. 이후 변경이 꼬이면 이 지점과 비교하거나, 이번 리팩터링 커밋만 `git revert`해서 되돌릴 수 있다.
- 기존의 **기준단가 → 별도 배송표** 두 화면을 한 기준가격 표로 합쳤다. 각 행에서 `판매가`, `배송 정책`, `가격 조정`, `현재 판매가`가 이어서 보인다.
- 세부 배송비·쿠폰·무료배송 계산은 삭제하지 않고 **배송 세부설정 팝업**으로 옮겼다. 팝업 값을 바꾸면 같은 상품코드의 기준가격 행과 몰별 현재 판매가가 즉시 다시 계산된다.
- 일반 아이소핑크 600×900의 `판매 시 5장당 배송비`는 팝업 상단의 공통값으로 편집한다. 공통값을 바꾸면 전 규격의 배송비 차액·1장당 차액이 자동 재계산되고, 규격별 실제 5장당 배송비와 판매가 조정금액은 각 행에서 편집한다.
- 판매가는 상단 공통 버튼 대신 각 판매가 셀의 금액 또는 연필 버튼을 눌러 해당 행만 편집한다. 최초 가격과 달라지면 셀 아래에 `변경 전 금액`과 되돌리기 버튼을 표시한다.
- 몰 화면은 아이소핑크 내부 단계에서 분리해 **채널 중심 전상품 화면**으로 바꿨다. `한국단열 → 전체/제품 종류`로 필터링하며, 앞으로 스티로폼·열반사단열재·단열벽지 등 한국단열에서 판매할 모든 제품을 같은 화면에 추가할 수 있다.
- 몰 상품 데이터의 상수명을 `HK_CHANNEL_LISTINGS`로 일반화하고 상품마다 `categoryId`를 넣었다. 현재 한국단열 아이소핑크 23상품·92옵션은 `hk_isopink`로 연결돼 있다. 다른 제품은 해당 제품의 기준가격 계산기와 `categoryId`를 추가하면 같은 표에 합쳐진다.
- 몰 비교표의 용어는 원래 목적대로 `현재 판매가`, `수정 전 판매가`, `차액`, `수정 전 배송비`로 표시한다.
- 회귀검사: 기준가격 83행, 한국단열 23상품·92옵션, 고유 상품코드 52개를 체크포인트와 비교했다. 현재 판매가와 기존 몰 옵션 결과 차이 0건, 미해결 코드 0건. 브라우저에서 통합 표·배송 팝업·한국단열 전체/아이소핑크 필터 렌더링도 확인했다.

### 다음 제품 연결 방식

1. 제품의 원가·사이즈별 기준 판매가와 배송 정책을 한 행 모델로 만든다.
2. 몰 상품 블록에 `categoryId`, 상품ID, 상품코드, 현재 가격·배송 정보를 넣는다.
3. `_hkChannelTargetPrice()`에 해당 제품의 상품코드 → 현재 판매가 resolver를 연결한다.
4. 그러면 한국단열의 `전체` 목록과 제품 종류 필터에 자동으로 포함된다.

> 푸시는 사용자가 요청할 때만 한다. 2026-09-21에 사용자가 요청해서 리팩터링·Supabase 저장·헤드/탭 개편 작업분까지 `origin/main`에 푸시했다. 현재 푸시 상태는 `git log origin/main`으로 확인한다.

## ⭐ 다음에 할 일: 나머지 채널 + 나머지 카테고리

**한국단열 채널의 아이소핑크 몰 상품은 2026-09-21에 다 채웠다**(상품번호 23개·옵션 92개, 엑셀과 전부 일치 확인). 남은 건:

1. **아이소핑크 몰 상품 입력 현황**: 한국단열(23상품·92옵션)과 **홈페이지(21상품·39옵션, 2026-09-21 입력 — 엑셀의 현재 판매가/옵션추가금/가격차이 39행 전부 일치, 코드 매칭 실패 0)** 완료. **한국단열라이프는 아이소핑크가 없다(사용자 확인)** — 스티로폼 등 다른 제품 때 다시 본다. **ESM(37상품·37옵션, 2026-09-21 입력 — 원본 표에는 "EMS"로 적혀 있음)**도 완료: ESM은 표 모양과 가격 규칙이 달라서 별도 레이아웃(`_hkEsmCategoryTableHtml`)과 채널 설정(`HK_CHANNEL_CONFIG.esm`)을 만들었다 — **ESM 최종 판매가 = (한국단열 판매가 + 한국단열 배송비) × 1.08 을 100원 단위 올림**(정수로 계산해 43,200 같은 딱 떨어지는 값이 100원 올라가는 소수 오차 방지). 한국단열 배송비는 한국단열 채널의 같은 상품코드 `baseShipping`을 자동으로 찾는다(600x430·900x1800은 0). 행마다 마스터상품번호(`masterId`)·상품번호(`productId`)가 따로 있고 옵션 하나가 상품 하나이며 그룹명(`groupName`)으로 묶는다. 데이터는 `HK_ESM_ROWS` 배열. 엑셀 37행의 한국단열 판매가/배송비/총판매가/ESM 판매가/최종 판매가가 계산값과 전부 일치, 수정 전 판매가 = 현재 최종 판매가. 시트의 상품명 1건(`Iso_600_900_10_3`이 "430x430 10T_3장"으로 적힘)은 시트 오타로 보이며 화면 상품명은 코드에서 만든다. ESM 상품번호는 지마켓 상품코드라 링크는 `https://item.gmarket.co.kr/Item?goodscode=상품번호`(`_hkChannelProductLink`의 `esm`, 사용자 제공 — ESM은 정확히는 지마켓 링크). **11번가(7상품·63옵션, 2026-09-21 입력)**도 완료: ESM과 같은 가격 규칙(`HK_CHANNEL_CONFIG['11st']` — (한국단열 판매가+배송비)×1.08을 100원 올림)이고, 표는 상품 하나에 옵션 여럿인 별도 레이아웃(`_hkMarkupOptionsTableHtml` — 기준가 옵션 라디오·옵션가 계산·파란 칸). **기준가 옵션이 첫 옵션이 아닌 상품**이 있어 코드에 미리 지정했다: 1534504863·1541883159 → `Iso_600_900_20_1`(11,200), 2221701321 → `IsoA_600_900_20_1`(14,200), 3666826849 → `Iso_900_1800_30_3`(53,500). 이런 코드상 지정(`baseCode`)은 **기본값(`seedBaseCode`)**이라 DB에 저장하지 않고, 사용자가 화면에서 다른 옵션으로 바꾼 것만 `channel_options`에 저장된다(이력 복원 시 기본값으로 돌아감). 1534504863과 1541883159는 옵션 구성이 같은 별개 상품(원본 그대로). 엑셀 63행의 기준가·옵션가·최종 판매가·배송비가 계산값과 전부 일치, 수정 전 판매가 = 현재 최종 판매가. 11번가 상품 링크는 `https://www.11st.co.kr/products/상품번호`(`_hkChannelProductLink`의 `11st`, 사용자 제공). **쿠팡(18상품·76옵션, 2026-09-21 입력 — 처음 준 일반 상품 15상품·39옵션 + 위너 상품 3상품·37옵션을 다시 받은 전체 표로 보강. 아래 설명 중 일반 상품 규칙은 그대로이고, 위너 상품 규칙·추가 열은 이 문단 끝의 "쿠팡 위너" 참고)**: 별도 레이아웃(`_hkCoupangCategoryTableHtml`)과 가격 규칙(`HK_CHANNEL_CONFIG.coupang`, `_hkCoupangPriceParts`) — **쿠폰 적용 전 판매가 = 총판매가(한국단열 판매가+배송비)×1.16 을 100원 올림**(초록 열 = 쿠팡에 등록하는 가격으로 보고 수정 전 판매가와 이것을 비교), 참고 판매가 = 총판매가×1.05 100원 올림, **쿠폰 적용 최종 판매가 = 쿠폰 적용 전 − 쿠폰 할인율** — 쿠폰은 상품마다 **10%를 먹이기도 하고 12%를 먹이기도 한다**(사용자 설명). 표 39행에서는 한국단열 배송비가 있는 상품(19옵션)이 10%, 없는 상품(20옵션)이 12%라서 기본값을 그렇게 두고(`couponOffShipping`/`couponOffFree`), 표에 **`쿠폰 할인율` 칸**(10% 파랑/12% 주황)을 추가했다. 옵션마다 `couponOff`(10|12)를 적으면 그 옵션만 그 할인율을 쓴다(화면에서 고치는 UI는 아직 없음). **쿠팡 표 재설계(2026-09-21, 21열)**: 아이템위너는 다른 판매자와 가격을 맞추려고 판매가를 직접 정하기도 해서, 열을 `총판매가 → 판매가(×1.05 계산) → 쿠폰 적용 전 → 쿠폰 할인율 → 쿠폰 적용 최종 → **수동 판매가**(위너 옵션만 입력, 비우면 계산가) → **등록 판매가**(쿠팡에 실제 입력하는 가격, 초록 강조는 이 칸 하나) → 수정 전 판매가 → 차액 → 재고/배송비/제주/교환반품 → **메모**`로 나눴다. 등록 판매가 = 일반은 쿠폰 적용 전 판매가, 위너는 수동 판매가(있으면) 아니면 계산한 판매가 — 수정 전 판매가·차액·반영 대기는 이 등록 판매가 기준. 수동 판매가가 있는 옵션은 "수동" 표시와 "계산가 ±N", 채널 헤드에 `수동 판매가 N` 배지. 메모는 쿠팡 모든 옵션에 남길 수 있다(글자를 치는 동안 표를 다시 그리지 않음). 수동 판매가·메모는 `hk_settings.channel_options`(`manualPrice`/`memo`, 상품코드 키)에 저장돼 SQL 변경 없음. **위너 옵션의 실제 쿠팡 가격은 아직 입력 전**이라 지금은 전부 계산가(×1.05)이고, 수동으로 정한 위너 가격을 알려주면 수동 판매가로 넣는다. 쿠팡 표에서 쿠폰 적용 전 판매가 칸의 강조는 없앴다(등록 판매가 칸으로 옮김).
- **쿠팡 표 틀 고정(2026-09-21)**: 쿠팡 표만 **상품코드 열까지**(Product ID·옵션 ID·상품명·상품코드 4열)를 좌우 스크롤해도 남긴다(엑셀의 틀 고정). 처음엔 5개 채널 전부 고정했다가 "쿠팡 말고는 표가 길지 않아 없는 게 낫다"는 지시로 쿠팡만 남겼다(ESM·11번가 열 폭도 원래대로 복원). `_hkFreezeListingColumns`가 표를 다시 그릴 때마다 `position:sticky`와 열별 `left`를 계산해 붙인다(세로 병합된 Product ID 칸이 있는 행도 열 번호로 맞춤). 창이 좁으면 고정 폭(645px)이 커서 스크롤 영역이 좁아진다 — 넓은 화면 기준.
- **쿠팡 가격의 의도(사용자 설명, 고치지 말 것)**: 쿠팡은 전부 무료배송이라 (네이버 판매가+배송비)에 수수료분 5%를 더한 ×1.05가 원래 쿠팡 판매가인데, 쿠폰을 쓰려고 정가를 ×1.16으로 등록하고 상품별 쿠폰(10%/12%)을 먹여 최종가를 ×1.05 근처로 맞춘다. **큰 사이즈·비싼 제품은 일부러 12%**를 줘서 최종가가 ×1.05보다 2~3% 낮고, 그에 맞춰 1.16을 1.18로 바꾸는 식의 조정은 너무 복잡해서 일부러 하지 않았다 — 그래서 10% 상품은 ×1.05와 거의 같고 12% 상품은 조금 낮은 게 정상이다. 행마다 옵션 ID(`optionId`)가 따로이고 Product ID(`productId`) 아래 옵션 여럿(옵션마다 가격이 독립이라 기준가·옵션가 개념 없음). 엑셀 39행의 네이버판매가/배송비/총판매가/쿠폰 적용 전/판매가/최종이 전부 일치. **쿠팡전용 접착식 600x900(IsoA_600_900_10_10·20_5·30_3·40_2·50_2, Product ID 5202287414)은 한국단열에 없는 코드**라 한국단열 배송비를 2단계 배송 블록(`_hkIsoBlockShippingByCode`, per5 블록의 기준 배송비 6,000)에서 대신 가져온다. 쿠팡 링크(사용자 제공 2026-09-21): Product ID(노출상품ID)는 `https://www.coupang.com/vp/products/상품ID`, 옵션 ID(vendorItemId)는 뒤에 `?vendorItemId=옵션ID`를 붙여 그 옵션이 선택된 채 열린다(`_hkChannelProductLink`의 `coupang`, `_hkCoupangOptionLink`). **쿠팡 위너(2026-09-21 추가)**: Product ID 9474591055(25옵션)·8202426484(6)·8080424483(6)은 위너 상품(`pricing:'winner'`, 이름이 "위너_/위너아님_")이라 **쿠폰 할인이 없고 판매가 = 총판매가×1.05 를 10원 단위 반올림(5는 올림)**, 그 판매가가 곧 최종 판매가이자 **등록 가격**(수정 전 판매가와 비교하는 값)이다. **위너는 쿠폰을 먹이지 않는다(사용자 확인) — **자동 판매가(수동 판매가를 비웠을 때)는 총판매가×1.05를 1,000원 단위 올림**(2026-09-21 사용자 지시, `winnerRoundUnit: 1000`; 예전엔 10원 반올림)** — 그래서 위너 행은 쿠폰 적용 전 판매가 칸을 "—", 쿠폰 할인율 칸을 "쿠폰 없음"(회색)으로 비우고, 판매가만 초록으로 강조하며 최종 판매가는 같은 숫자를 또 강조하지 않고 평범하게 보여준다(엑셀에 있던 위너의 쿠폰 적용 전 값은 화면에서 뺐다). 위너 옵션은 이름을 코드로 만들 수 없어 직접 적었고(`_hkWinnerName`), 코드는 `IsoC_…`와 일부 `Iso_900_1800_…`가 섞여 있다. 표에 수수료(10.8%)·재고수량(99,999)·쿠팡 배송비(무료)·제주배송비(8,000)·교환/반품(상품별 5000/10000/20000/40000) 열도 추가했다. 전 76옵션의 네이버 판매가/배송비/총판매가/쿠폰 적용 전/판매가가 시트와 일치하는데, **배송비는 5장 기준(사용자 확인 2026-09-21): 1~5장 6,000 / 6~10장 12,000 / 20장 24,000.** 그래서 위너 10T 600x900 10장·20장 3행은 엑셀의 6,000 / 12,000(틀린 값으로 정리)이 아니라 배송 설정 값 12,000 / 24,000을 따른다(코드의 `hkdShipping` 덮어쓰기 제거). 이 3행은 수정 전 판매가를 엑셀 값(32,030 / 63,840 / 32,030) 그대로 둬서 새 등록 판매가(38,330 / 76,440 / 38,330)와의 차액(+6,300 / +12,600 / +6,300)이 `반영 대기 3`으로 뜬다 — 스토어 가격을 고친 뒤 저장하면 사라진다. 쿠팡전용 접착식 `IsoA_600_900_10_10`(Product ID 5202287414, 10장)도 같은 규칙으로 배송비 12,000원(엑셀은 6,000)이 되게 고쳤다 — `_hkIsoBlockShippingByCode`가 행에 기준배송비가 따로 없으면 블록의 5장당 배송비 × 박스 수(장수÷5 올림)로 계산한다(등록 판매가 62,700 → 69,600, 수정 전 판매가는 엑셀 값이라 차액 +6,900). 이 4행이 `반영 대기 4`로 뜬다. 쿠팡 전 옵션이 5장 기준 배송비 규칙에 맞는 것을 확인했다. 남은 채널: **쿠팡_부자재**(입력 전). **새 채널의 수정 전 판매가·배송비는 엑셀의 예전 값이 아니라 현재 판매가·배송비에 맞춰 넣는다(사용자 지시 2026-09-21, 앞으로도 계속)** — 홈페이지도 그렇게 맞췄다(차액 0에서 시작, 반영 대기 0). 홈페이지는 상품ID가 홈페이지 상품번호(187 등)이고 상품 링크는 `https://boonimall.kr/goods/view?no=상품번호`(`_hkChannelProductLink`의 `homepage`, 2026-09-21 사용자 제공). 나머지 채널(쿠팡/ESM/11번가 등)은 링크 주소 패턴을 받으면 같은 함수에 추가. 한국단열·홈페이지·ESM·11번가·쿠팡 5개 채널 데이터는 사용자가 2026-09-21에 저장해서 DB에 다 올라가 있다(상품 106개·옵션 307개 확인). 몰 상품 엑셀(상품명/상품코드/상품ID/현재 판매가/기준가/옵션추가금/재고수량/배송비/배송비기준/제주배송비/편도교환/수정 전 판매가/차액/수정 전 배송비)을 받으면 `HK_CHANNEL_LISTINGS`에 채널 키를 추가.
2. **아이소핑크 말고 카테고리 6개**(스티로폼/열반사단열재/단열벽지/창문형단열재/기타단열재/부자재)는 기준가격과 몰 상품 연결이 아직 없음. 각 제품도 `기준가격+배송` 통합 모델과 `categoryId` 연결로 추가한다.
3. Supabase 저장은 구현됨(맨 위 절 참고) — **`sql/hankook_pricing_tables.sql` 실행 + 첫 "저장"이 아직 안 됨**.

**이어서 할 때 방법**: 패턴은 이미 있는 `hkd` 채널 그대로 따라하면 됨 — `categoryId`, `product.items[].prevPrice`(수정 전 판매가)와 상품 단위 값(배송비/배송비기준/제주/편도교환)을 넣고, 제품별 현재 판매가 resolver를 연결하면 기준가/옵션추가금/차액은 자동 계산됨.

### 2026-09-21: 한국단열 채널 3단계 상품 7개 추가 (Claude)

`HK_ISO_CHANNEL_LISTINGS.hkd`에 상품번호 `3020442618`(6옵션), `439904706`(16), `3736232926`(5), `442086644`(4), `4995022274`(5), `5695312387`(12), `5697937041`(4) 추가 — 기존 `439103571`·`2229818356`(각 12)과 합쳐 **9개 상품·76옵션**. 신규 52행을 엑셀의 네이버판매가/기준가/옵션추가금/수정 전 판매가/가격차이/배송비/배송비기준/제주/편도교환/수정전배송비와 브라우저에서 전부 대조해 일치 확인, 상품코드 매칭 실패 0건.

**같은 날 2차 추가(단품·600 계열 고티, 상품 14개·16옵션)**: 900x1800 단품은 **옵션 하나가 상품 하나**라 상품ID가 옵션마다 따로다 — 일반 `8324406068`(10T)·`8456757485`(20T)·`11097629335`(30T)·`8324375715`(40T)·`8131395351`(50T)·`8324347562`(70T)·`8324352040`(100T), 접착식 `10181453964`(10T)·`10181564057`(20T)·`10181571912`(30T)·`10181582241`(40T)·`10181586522`(50T). 이 상품들은 배송비/배송비기준/제주/편도교환도 **상품마다 다르다**(예: 10T는 10,400·5개마다·21000/42000, 50T는 제주 30,000). 600 계열 고티는 상품 하나에 옵션 2개(600x430, 600x860): `10185646787`(250T)·`10185649832`(500T), 기준가는 첫 옵션(600x430). 신규 16행 엑셀 대조 일치·링크 정상·코드 매칭 실패 0. 이걸로 한국단열 채널은 **상품번호 23개·옵션 92개**.

**이번에 새로 확인된 규칙 2가지**:
- **기준가 = 그 상품의 "첫 번째 옵션" 가격**이지 최저가가 아니다. 3736232926(첫 옵션 12,600인데 7,100짜리 옵션이 있음)·5695312387(첫 옵션 55,000, 37,000짜리 있음)에서 옵션추가금이 **음수**로 나오는 게 엑셀 원본 그대로였다. 처음엔 최저가로 잡아서 이 두 상품에서 틀렸을 것 — 코드를 "첫 옵션 기준"으로 바로잡음(기존 두 상품은 첫 옵션=최저가라 결과 동일).
- `442086644`만 "수정 전 배송비"가 6,500(현재 배송비 6,000과 다름) — 옵션별 `prevShipping`으로 그대로 반영.
- 상품코드 이름 변환(`_hkIsoProductNameFromCode`)이 `IIso_`(I 두 번, 실제 등록된 오타 코드)도 처리하도록 수정.
- **2026-09-21 Codex 재검토**: 83개 기준단가 행 중 분할 원가가 소수로 생기는 행은 화면 표시만 원 단위로 반올림하고, 마진·장당마진·순수마진율 계산에는 원본 소수 원가를 유지하도록 수정. 예: 2,500원 ÷ 8 × 3 = 937.5원(표시 938원), 2,600원 판매 시 마진 1,663원. 판매가를 편집해도 원본 엑셀 값과 1원 어긋나지 않음.

**이어서 할 때 방법**: 사용자가 엑셀에서 "상품명/상품코드/상품ID/네이버판매가/기준가/옵션추가금/재고수량/배송비/배송비기준/제주배송비/편도교환/수정 전 판매가/가격차이/수정전 배송비" 표를 캡처해서 주면, `js/pricing-hankook.js`의 `HK_ISO_CHANNEL_LISTINGS`에 그 채널·상품번호 블록을 추가하면 된다(패턴은 이미 있는 `hkd` 채널 예시 그대로 따라하면 됨 — `product.items[].prevPrice`만 실제 값으로 채우면 네이버판매가/기준가/옵션추가금/가격차이는 전부 자동 계산됨).

## 2단계는 다 끝났음 (더 안 건드려도 됨)

아이소핑크 9개 사이즈/판매방식 조합(일반 600x900·600 계열 고티·900x1800 단품/묶음/고티, 접착식 600x900·900x1800 단품/묶음, 쿠팡 위너) 전부 원본 엑셀 실측값 반영 완료, 코드 매칭 전수 검사(중복 0건·미해결 0건)까지 확인함. 아래 "900x1800/600x900 고티 2단계 나머지 전부 반영"·"쿠팡 위너 2단계" 절 참고.

## 현재 Git 상태 (2026-09-16 기준)

- 커밋 `255b4eb`로 전부 반영·푸시 완료(`main`, GitHub origin).
- 이 시점 이후 로컬 작업트리는 깨끗한 상태(uncommitted 변경 없음).
- 다음 작업(3단계 데이터 추가)부터는 새 커밋으로 쌓으면 됨.

## 현재 구현 완료 사항

### 1. 한국단열 아이소핑크 새 단가표

`js/pricing-hankook.js`에 다음 9개 기준단가 그룹이 모두 데이터와 계산식까지 연결돼 있다.

| 내부 키 | 현재 화면명 | 행 수 |
|---|---|---:|
| `iso_600x900` | 아이소핑크 600x900 | 14 |
| `adhesive_600x900_a` | 접착식 아이소핑크 600x900 | 10 |
| `iso_900x1800_bundle` | 아이소핑크 900x1800_묶음 | 7 |
| `iso_900x1800_single` | 아이소핑크 900x1800_단품 | 7 |
| `iso_900x1800_high_t` | 아이소핑크 900x1800_고티 | 4 |
| `adhesive_900x1800_single` | 접착식 아이소핑크 900x1800_단품 | 5 |
| `adhesive_900x1800_bundle` | 접착식 아이소핑크 900x1800_묶음 | 5 |
| `iso_600x900_high_t` | 아이소핑크 600x900_고티 | 5 |
| `iso_coupang_winner` | 아이소핑크_쿠팡 위너 | 26 |

~~현재 화면은 위 9개가 각각 가로 하위 탭으로 노출된다.~~ → **(완료) 3개 상위 탭(일반/접착식/쿠팡 위너) + 아코디언 구조로 변경됨. 맨 아래 절 참고.**

### 2. 공통 원가 구조

에너가드 아이소핑크 방식처럼 `두께구간 기본 원가 + 두께별 추가마진`으로 변경했다.

기본 원가:

- 10T~20T: 260원/mm
- 30T~180T: 198원/mm
- 185T 이상: 201원/mm

추가마진 기본값:

| 두께 | 추가마진 | 적용 원가 |
|---:|---:|---:|
| 10T | -10 | 250 |
| 20T | -10 | 250 |
| 30T | +27 | 225 |
| 40T | +27 | 225 |
| 50T | +22 | 220 |
| 70T | +22 | 220 |
| 100T | +22 | 220 |
| 250T | +29 | 230 |
| 500T | +29 | 230 |

- 공통 원가를 바꾸면 모든 기준단가 그룹이 함께 다시 계산된다.
- 접착식은 계산된 원장 원가에 원장당 가공비 1,350원을 추가한다. 가공비를 바꾸면 접착식 전 행의 원가·판매원가·예상판매가가 다시 계산된다.
- 마진 편집은 펼침 영역이 아니라 에너가드와 같은 별도 팝업이다.
- 팝업 HTML은 `index.html`의 `#hkIsoMarginModal`에 추가했다.
- 마진 편집 버튼은 접착 가공비 입력란 바로 옆, 원가표 상단 왼쪽에 배치했다.

### 3. 판매가 표 기능

- 참고마진을 기준으로 예상판매가를 100원 단위로 자동 계산한다.
- 최종 판매가는 고정 표시되며 `판매가 편집` 버튼을 눌러야 수정할 수 있다.
- 판매원가, 마진, 판매수수료 6%, 부가세 10%, 배송비, 장당마진, 순수마진율을 계산한다.
- 고티 행은 배송비도 예상판매가와 순수마진 계산에서 차감한다.
- 계산 셀에 마우스를 올리면 산출식을 `title` 툴팁으로 표시한다.
- 원본 표의 보조 `판매가/원장 판매가` 열은 데이터 보존 목적으로 남기되 불투명도를 낮췄다.
- `600x430`은 원장 6분할, `600x860`·`600x900`은 3분할, `430x430`은 8분할, `900x1800`은 1분할로 계산한다.

### 4. 화면 진입과 정리

- 한국단열로 진입할 때 아이소핑크 첫 화면으로 초기화하도록 `js/pricing.js`의 `setPricingCompany('hkd')`에서 `resetHkPricingView()`를 호출한다.
- 숨겨 보관하던 구형 한국단열 아이소핑크 렌더러, 시드 데이터, 구형 계산 함수와 전용 CSS를 모두 삭제했다.
- (2026-09-21 이후) 저장/불러오기는 `js/pricing-hankook-db.js`가 맡는다. 맨 위 "Supabase 저장" 절 참고.

## (완료) 3개 상위 탭 + 아코디언 — 원래 계획

사용자 최종 요청은 기존 9개 가로 탭을 다음 **3개 상위 탭**으로 합치는 것이었다. 아래는 원래 계획이고, 실제 구현 내용은 이 문서 맨 아래 "3개 상위 탭 + 아코디언 구현 완료 (Claude)" 절에 있다.

### 일반 아이소핑크

아코디언 섹션:

1. 600x900 일반 — `iso_600x900`
2. 600 계열 고티(실제 판매사이즈 600x860·600x430) — `iso_600x900_high_t`
3. 900x1800 단품 — `iso_900x1800_single`
4. 900x1800 묶음 — `iso_900x1800_bundle`
5. 900x1800 고티 — `iso_900x1800_high_t`

### 접착식 아이소핑크

아코디언 섹션:

1. 600x900 — `adhesive_600x900_a`
2. 900x1800 단품 — `adhesive_900x1800_single`
3. 900x1800 묶음 — `adhesive_900x1800_bundle`

### 쿠팡 위너

아코디언 섹션:

1. 쿠팡 위너 전체 — `iso_coupang_winner`

구현 원칙:

- 한 상위 탭 안에서 아코디언 하나만 펼친다.
- 상위 탭을 바꾸면 해당 탭의 첫 아코디언을 기본으로 연다.
- 아코디언 머리에는 `기준단가 N개`를 표시한다.
- `판매가 편집`은 현재 펼친 아코디언 안의 판매가만 편집 가능하게 한다.
- `HK_ISO_CONNECTED_DRAFTS`의 기존 내부 키는 이후 상품 매핑의 기준키로 사용해야 하므로 변경하지 않는다.
- 현재 9개 표 데이터와 계산 함수는 그대로 재사용하고 렌더 구조만 바꾼다.

## 옵션 중복 확인 결과

쿠팡 위너를 제외하고 `상품 종류 + 두께 + 판매사이즈 + 묶음수량`을 옵션 기준으로 보면 일반 아이소핑크에 중복 2개가 있다.

| 기준단가 그룹 | 옵션 | 최종 판매가 |
|---|---|---:|
| 900x1800 묶음 | 70T · 900x1800-1 | 26,500원 |
| 900x1800 단품 | 70T · 900x1800-1 | 27,000원 |
| 900x1800 묶음 | 100T · 900x1800-1 | 35,500원 |
| 900x1800 단품 | 100T · 900x1800-1 | 36,500원 |

나머지는 일반/접착식 구분을 포함하면 중복이 없다. 실제 상품과 연결하기 전에 70T·100T 1장 상품을 단품과 묶음 중 어느 기준으로 연결할지 사용자가 결정해야 한다. 같은 상품번호·옵션에 두 기준단가를 동시에 연결하면 안 된다.

## 이후 실제 상품 연결 계획

현재 표는 실제 스토어 판매가가 아니라 **상품 가격을 만들기 위한 기준단가표**다. 화면 그룹과 상품 매핑을 분리한다.

1. 각 기준단가 행에 안정적인 `price_key`를 부여한다.
   - 예: `iso_900x1800_single__70__900x1800-1`
   - 구성: `기준단가 그룹 키 + 두께 + 판매사이즈/묶음수량`
2. 실제 상품 매핑에는 최소한 다음 정보를 저장한다.
   - `price_key`
   - 상품번호
   - 옵션 ID 또는 옵션명
   - 판매 채널/스토어
   - 활성 여부
3. 화면 이름이나 아코디언 배치를 바꿔도 `price_key`는 바꾸지 않는다.
4. 실제 상품 가격은 기준단가와 별도 값으로 수집한다.
5. 이후 검사 화면에서 `기준단가 / 실제 판매가 / 차액 / 일치 여부`를 비교한다.
6. 아코디언 머리에는 향후 `연결상품 N개`, `가격검사 정상/불일치` 요약을 추가할 수 있다.

저장 방식은 아직 결정하지 않았다. 상품 연결을 시작하기 전에 한국단열 기준단가와 매핑을 저장할 Supabase 테이블 구조를 정해야 한다.

## 기존 스토어 가격검사 관련 참고

- 스토어 가격검사와 경쟁사 가격 확인 기능은 별도로 구현돼 있다.
- 실제 로드되는 확장프로그램은 별도 레포 `../Naver-rank/shopping-rank-extension/`이다.
- 통합 빌드/복사는 `node tools/integrate-price-test.cjs`를 사용한다.
- 한국단열 상품 연결 후 기존 `getTablePrice`, 옵션 매핑, 가격검사 구조를 재사용할 수 있다.

## 3개 상위 탭 + 아코디언 구현 완료 (Claude, 2026-09-16)

`js/pricing-hankook.js`에서 기존 9개 가로 하위 탭(`HK_ISO_DRAFT_TABS`)을 삭제하고, 3개 상위 탭 안에 아코디언을 두는 구조(`HK_ISO_SUPER_TABS`)로 교체했다. `HK_ISO_CONNECTED_DRAFTS`의 키(내부 id)는 계획대로 손대지 않았다.

- **상위 탭 3개**: 일반 아이소핑크 / 접착식 아이소핑크 / 쿠팡 위너 — `#hkIsoSuperTabBar`에서 `setHkIsoSuperTab(superId, el)`로 전환.
- **아코디언**: 각 상위 탭 안에 계획대로 섹션 배치(일반 5개, 접착식 3개, 쿠팡 위너 1개). 헤더에 `기준단가 N개`(= 해당 기준단가표의 행 수) 표시. `toggleHkIsoAccordion(accId)`로 그 아코디언 하나만 독립적으로 열고 닫는다 — 라디오 방식이 아니라 진짜 토글이라 여러 개를 동시에 펼 수도, 다 접을 수도 있다(2026-09-16 사용자 피드백으로 라디오 방식에서 변경).
- **상위 탭 전환 시**: 아코디언 상태를 건드리지 않는다 — 탭을 오갈 때 각 탭에서 열어뒀던 아코디언 상태가 그대로 유지된다.
- **`resetHkPricingView()`만 예외적으로 강제 초기화**: 한국단열에 새로 진입할 때만 첫 상위 탭(일반)의 첫 아코디언(600x900 일반)만 열고 나머지는 닫은 깨끗한 상태로 되돌린다 — "첫 진입 시 맨 앞이 보여야 한다"는 기존 요구사항은 그대로 유지.
- **판매가 편집 범위**: `_setHkIsoFinalPriceEditing()`가 `.hk-iso-super-pane.active .hk-iso-accordion.open` 안의 입력칸만 활성화한다 — 다른 아코디언/탭의 판매가는 편집 버튼을 켜도 여전히 잠겨 있다. 상위 탭이나 아코디언을 바꾸면 편집 모드는 자동으로 꺼진다.
- **`resetHkPricingView()`**: 한국단열 진입 시 항상 첫 상위 탭(일반 아이소핑크)의 첫 아코디언(600x900 일반)을 연다 — `js/pricing.js`의 `setPricingCompany('hkd')`에서 계속 호출.
- 표 자체(`_hkIsoDraftSalesTable`, 계산 로직, 마진 팝업 등)는 전혀 건드리지 않았다 — 바깥 탭/아코디언 껍데기만 바꿨다.
- CSS: `css/pricing.css`에서 안 쓰이게 된 `.hk-iso-draft-tabs`/`.hk-iso-draft-pane`을 지우고 `.hk-iso-super-pane`/`.hk-iso-accordion*`로 교체.
- 로컬 정적 서버(python http.server)로 실제 렌더 확인 완료: 상위 탭 전환, 아코디언 단일 열림, 판매가 편집 범위 제한(개발자 콘솔로 활성화된 입력칸 개수 직접 카운트해서 검증)까지 정상 동작.
- **아직 커밋 안 함.**

### 남은 것 (변경 없음, MD 원안 그대로 유효)

- 옵션 중복 확인 결과(70T·100T 900x1800 단품 vs 묶음) — 사용자 결정 필요, 손대지 않음.
- 실제 Supabase 매핑 테이블 연결 — 착수 전.

## 상품코드 열 추가 (Claude, 2026-09-16)

사용자가 실제 스마트스토어 등록 상품코드 목록(`Iso_430_430_10_3` 형태)을 주고 "1단계 표에서 완성하고 그 값을 네이버 상품코드와 연동"하기로 방향을 정했다. 2단계(배송비 보정)·3단계(네이버 옵션추가금) 표를 따로 만들지 않고, 1단계 기준단가표 안에서 전부 끝내는 방향.

- `_hkIsoDraftProductCode(saleSize, thickness, isAdhesive)`를 추가해 판매사이즈·두께·수량으로 코드를 기계적으로 생성한다: `(일반)Iso_ / (접착식)IsoA_` + 규격(`430_430` 등) + 두께 + 수량. 예: 10T·430×430-3장 → `Iso_430_430_10_3`.
- `_hkIsoDraftSalesTable()`의 "판매사이즈" 열 바로 뒤에 "상품코드" 열을 추가했다(쿠팡 위너 제외 — 이 코드 체계를 안 쓰는 걸로 보여서 뺐음, 확인 필요하면 사용자에게 물어볼 것). `<tr data-product-code="...">`로도 심어둬서 나중에 실제 상품 매핑(Supabase)의 키로 그대로 쓸 수 있게 해뒀다.
- 사용자가 준 코드 50개(중복 제거)를 브라우저에서 직접 대조해서 **전부 정확히 일치**하는 것 확인했다(콘솔에서 `missingFromActual: []`). 생성된 코드 중 사용자 목록에 없던 5개(`IsoA_600_900_10_10` 등)는 접착식 600x900의 두 번째 수량 변형 행(엑셀엔 있지만 아직 코드 목록엔 안 올라온 것으로 보임) — 버그 아님, 사용자 확인 필요하면 물어볼 것.
- **아직 커밋 안 함.** 이 문서 상단의 "다음 작업자"가 이어서 진행하면 된다.

## 2단계(상품번호별 배송비/실판매가) 착수 — 원가 3단계 구조 확정 (Claude, 2026-09-16)

사용자가 원본 엑셀 `★단가표수정\한국단열\나눔\아이소핑크_실제단가.xlsx`(시트: 수정단가)를 확인해보라고 해서 열어봤고, **1단계 표 안에서 2단계를 자동 흡수하는 건 불가능하다는 걸 확인**했다(이전에 내가 "2단계 표 필요없다"고 제안했던 게 틀렸음 — 정정함).

### 왜 안 되는지 (엑셀에서 확인한 사실)
- 같은 옵션(예: `Iso_430_430_70_1`)이 **서로 다른 상품번호 블록에 각각 따로** 들어있다 — 상품번호마다 실제배송비가 독립적으로 존재.
- **무료배송 여부에 따라 계산 공식 자체가 다르다**: 일반 상품은 `실판매가 = 판매가`(배송비 별도 청구), 무료배송 상품(예: 상품번호 `5695312387(무료)`)은 `실판매가 = 판매가 + 배송비플러스금액`.
- 900×1800 단품 블록은 아예 열 구성이 다르다(`1장당 배송비` / `배송비 무료` / `알림 쿠폰`), "실제배송비" 칸에 숫자 대신 "5개마다"류 규칙 텍스트가 들어있기도 함.
- 쿠팡 위너는 `IsoC_` 접두어를 따로 쓴다(이전에 "코드 없음"이라 판단한 게 오류였음 — 정정함, 아직 미구현).
- 코드 자체에 오타로 보이는 게 실사용 데이터에 있음: `IIso_900_1800_70_1` / `IIso_900_1800_100_1`(I가 두 번) — 사용자에게 확인 필요.

### 확정한 3단계 배치 방향 (사용자와 논의해서 결정)
1단계·3단계는 자주 보니 탭으로 바로 전환, 2단계는 자주 안 보니 버튼 눌러서 여는 섹션(현재는 같은 페이지 안에서 펼치는 방식으로 구현, 필요하면 나중에 탭으로 승격 가능).

### 지금까지 구현한 것 (`js/pricing-hankook.js`)
- **1단계 표의 상품코드 컬럼은 도로 제거함**(사용자 지시) — `_hkIsoDraftProductCode()` 헬퍼 함수 자체는 남겨두고 2단계에서 쓴다.
- **2단계 첫 번째 예시 구현**: `HK_ISO_SHIPPING_BLOCKS` 배열에 "아이소핑크 600x900"(상품번호 439103571 // 2229818356 // 3020442618) 블록 하나를 실제 엑셀 값 그대로 넣었다. 계산 관계:
  - `배송비차액 = 5장당실제배송비 − 5장당배송비(기준)`
  - `1장당배송비차액 = 배송비차액 ÷ 5`(참고용 — 실제 판매수량과 무관하게 항상 5로 나눔, 엑셀 역산으로 확인)
  - `배송비 플러스 금액`은 위 참고값을 보고 **사람이 100원 단위로 직접 정하는 값**이라 순수 계산으로 못 만든다 — 그래서 노란 배경의 직접입력 칸(`hk-iso-ship-plus-input`)으로 뒀다.
  - `실판매가 = 1단계 판매가 + 배송비 플러스 금액`
- 렌더: `_hkIsoShippingBlockHtml(block)`가 블록 하나를 표로 그리고, `recalcHkIsoShippingRow(input)`이 입력값 바뀔 때마다 배송비차액/1장당차액/실판매가를 다시 계산한다.
- UI: 아이소핑크 결과 카드 헤더에 "배송비·실판매가(2단계)" 버튼을 추가했고, 누르면 아래에 `#hkIsoShippingSection`이 펼쳐진다(기본은 숨김, `toggleHkIsoShippingSection()`).
- **검증 완료**: 브라우저에서 직접 렌더해서 14행 전부(`basePrice`/`배송비차액`/`1장당차액`/`실판매가`) 엑셀 원본과 정확히 일치하는 것 확인(`Iso_430_430_10_3`→2,400원 등 전부 일치).
- **아직 안 한 것**: 접착식 600x900 블록, 900x1800(단품/묶음/고티) 블록들, 무료배송 케이스(다른 공식), 쿠팡 위너(`IsoC_` 코드) 블록 — 전부 사용자가 다음 예시를 줄 때마다 `HK_ISO_SHIPPING_BLOCKS`에 블록을 추가하는 식으로 확장하면 됨. 3단계(실제 네이버 옵션 등록 확인)는 아직 손 안 댐.
- **아직 커밋 안 함.**

### 버그 수정: 2단계 판매가가 1단계와 진짜로 연동되지 않던 문제 (Claude, 2026-09-16)

사용자가 "2단계 판매가 데이터 1단계에서 받고 있는거 맞아?"라고 물어봐서 확인했더니 — **아니었다.** `_hkIsoShippingBlockHtml()`이 `HK_ISO_CONNECTED_DRAFTS[...].rows`(정적 시드 배열)의 `row.price`를 읽고 있었는데, 이건 1단계 화면의 `.hk-iso-final-price-input`(실제 입력칸, "판매가 편집"으로 사람이 바꿀 수 있는 값)과는 별개의 값이었다. 페이지를 막 열었을 때는 둘 다 같은 시드값이라 우연히 일치해 보였을 뿐, 1단계에서 판매가를 실제로 편집하면 2단계는 그 변경을 전혀 반영하지 못했다.

고쳤다:
- `_hkIsoShippingBlockHtml()`이 이제 `#hkIsoAcc-{accordionId} .hk-iso-final-price-input`을 순서대로 읽어서 그 값을 basePrice로 쓴다(DOM이 없는 최초 렌더 시점엔 시드값으로 방어적 대체).
- 2단계 섹션은 이제 페이지 로드 시 미리 그리지 않고, **"배송비·실판매가(2단계)" 버튼을 눌러 열 때마다 새로 그린다** — 그 시점의 1단계 값을 그대로 가져오기 위해.
- 혹시 2단계를 열어둔 채로 1단계 값을 바꾸는 경우를 위해, 각 블록 헤더에 "1단계 판매가 새로고침" 버튼도 추가했다.
- 브라우저에서 직접 검증: 1단계 10T·430×430-3 판매가를 9,999원으로 바꾸고 2단계를 다시 열었더니 판매가 9,999원, 실판매가 9,799원(=9,999−200)으로 정확히 반영됨을 확인.
- **아직 커밋 안 함.**

## 채널 선택 탭 추가 (Claude, 2026-09-16)

한국단열 카테고리 탭(아이소핑크|스티로폼|...|부자재) 옆에 `|` 구분선을 넣고 판매 채널 탭 7개(한국단열/한국단열라이프/홈페이지/쿠팡/쿠팡_부자재/ESM/11번가)를 추가했다.

- `HK_CHANNELS` 배열 + `_activeHkChannel` 상태 + `window.setHkChannel(channelId, el)` 추가.
- `initHkPricingTabs()`의 탭바를 `.pricing-tabs-row` 안에 `#hkCategoryTabs`(카테고리)와 `#hkChannelTabs`(채널) 두 그룹으로 나누고 그 사이에 `.hk-tab-divider`를 넣었다 — **두 그룹을 별도 컨테이너로 분리한 이유**: 둘 다 같은 `.pricing-tab` 클래스를 쓰는데, 한 그룹 안에서만 querySelectorAll로 active를 토글해야 카테고리를 바꿔도 채널 선택이 안 풀리고 그 반대도 마찬가지라서(처음엔 같은 `#hkPricingTabsBar` 하나로 스코프해서 만들려다 이 충돌을 미리 발견해서 구조를 나눔).
- **지금은 선택 상태만 있고 실제로 화면 내용을 채널별로 다르게 보여주는 연결은 없다** — 어떤 채널을 고르면 뭐가 달라져야 하는지는 사용자가 아직 얘기 안 함. 필요해지면 `window._activeHkChannel` 값을 참고해서 확장하면 됨.
- 브라우저에서 채널 클릭↔카테고리 클릭이 서로 안 건드리는 것 확인.
- **아직 커밋 안 함.**

## 3단계(채널별 실제 등록 상품) 첫 구현 — "한국단열" 채널 (Claude, 2026-09-16)

채널 탭을 만든 이유가 여기에 3단계 최종 상품데이터를 넣기 위해서였다는 걸 확인. 사용자가 실제 사용하던 엑셀(상품코드로 VLOOKUP해서 만들던 표)을 그대로 반영했다.

- `HK_ISO_CHANNEL_LISTINGS`(채널ID별 배열) 추가 — 지금은 `hkd`(한국단열)에 상품번호 439103571 / 2229818356 두 블록, 각 12개 옵션(일반 8 + 접착식 4)이 들어있다.
- `_hkIsoLookupFinalPriceByCode(code)` — 상품코드로 2단계(`HK_ISO_SHIPPING_BLOCKS`)에서 실판매가를 찾아온다(엑셀 VLOOKUP과 같은 방식). 1단계 DOM의 지금 값을 읽으므로 1→2→3단계가 전부 실시간으로 이어진다.
- 계산 규칙(엑셀 24행 전부 역산해서 확인):
  - 네이버판매가(기본가) = 상품코드로 찾은 2단계 실판매가
  - 기준가 = 같은 상품ID 안에서 제일 싼 네이버판매가
  - 옵션추가금 = 네이버판매가 − 기준가
  - 가격차이 = 네이버판매가(새로 계산한 값) − 수정 전 판매가(그 전에 실제 스토어에 있던 값, 사람이 확인해서 넣어둔 값)
- `_hkIsoProductNameFromCode(code)` — 상품코드 자체(`Iso_430_430_10_3` 등)에 규격·두께·수량이 다 들어있어서 상품명을 따로 안 적고 코드에서 만들어낸다.
- 채널 탭을 클릭하면 `setHkChannel()`이 `#hkChannelListingSection`을 그 채널 것으로 다시 그린다. 자료 없는 채널(한국단열라이프/홈페이지/쿠팡 등)은 "아직 입력 전" 안내만 뜬다.
- **부수적으로 발견해서 같이 고친 것**: 이 3단계 예시에 접착식 600x900 옵션(`IsoA_600_900_*`)이 있었는데, 2단계 쪽에 접착식 600x900용 배송비 블록이 아직 없어서 조회가 실패하고 있었다 — `HK_ISO_SHIPPING_BLOCKS`에 `adhesive_600x900_1` 블록을 새로 추가해서 해결(10T/30T/40T는 원본 엑셀 실측값, 20T는 배송비차액이 정확히 0이 되는 값, 50T는 이번 3단계 예시엔 안 쓰이지만 원본 엑셀에 있던 값 그대로 채워둠). 단, 접착식 600x900의 "묶음"행(10_10, 20_5, 30_3, 40_2, 50_2)은 아직 실측 배송비 자료가 없어서 기준배송비 그대로(plusAmount:0) 임시로 채워뒀다 — 나중에 실측값 받으면 교체 필요.
- 브라우저에서 검증: 24행 전부(네이버판매가/기준가/옵션추가금/가격차이) 엑셀 원본과 정확히 일치, 다른 채널은 정상적으로 "입력 전" 안내가 뜨는 것 확인.
- **아직 커밋 안 함.**

## 카테고리 탭 ↔ 채널 탭 화면 분리 (Claude, 2026-09-16)

사용자 요청: "한국단열 등 채널 탭에서는 기존 아이소핑크·스티로폼 같은 기본 가격 데이터가 안 나오게" — 카테고리 탭과 채널 탭을 같은 화면에 다 보여주던 걸, 둘 중 하나만 보이는 모드 전환으로 바꿨다.

- 1·2단계(공통 원가 입력 + 결과 카드 + 2단계 배송비 섹션)를 `#hkIsoBaseDataSection`으로 통째로 감쌌다.
- `setHkPricingTab()`(카테고리 탭 클릭) → `#hkIsoBaseDataSection` 보이고 `#hkChannelListingSection`(3단계) 숨김 = "카테고리 모드".
- `setHkChannel()`(채널 탭 클릭) → 반대로 `#hkIsoBaseDataSection` 숨기고 `#hkChannelListingSection` 보이고 새로고침 = "채널 모드".
- 기본 진입 상태는 카테고리 모드(아이소핑크, 기존과 동일) — `resetHkPricingView()`가 `setHkPricingTab()`을 호출하므로 자동으로 유지됨.
- 브라우저에서 검증: 채널 클릭 시 base=숨김/listing=24행 보임, 다시 카테고리 클릭 시 base=보임/listing=숨김으로 정확히 전환되는 것 확인.
- **아직 커밋 안 함.**

## 겹침 버그 2건 수정 (Claude, 2026-09-16)

사용자가 "겹치는 등 문제 해결해죠"라고 해서 실제로 화면을 스크린샷으로 확인해서 찾아냈다(이전엔 값만 JS로 대조했지 눈으로 직접 본 적이 없었음 — 그래서 놓쳤던 버그).

1. ~~카테고리 탭 줄 ↔ 채널 탭 줄이 붙어 보이는 문제~~ — **되돌림(아래 참고)**: 사용자가 이 부분은 문제 제기한 적 없다고 해서 원래대로(한 줄 + 세로 구분선) 복원함.
2. **3단계 표의 "상품명"·"상품코드" 글자가 서로 겹치는 문제(진짜 버그)**: `.hk-iso-listing-table`에 `<colgroup>`(열 너비 지정)이 없는 상태로 `table-layout: fixed`(이 앱 표 전체에 적용된 규칙)를 쓰다 보니, 14개 칼럼 너비가 내용과 상관없이 균등 분배돼서 "아이소핑크 430x430 10T_3장"처럼 긴 상품명이 좁은 칸에 눌려 옆 칸(상품코드) 글자와 겹쳐 보였다. `<colgroup>`으로 칼럼별 너비를 직접 지정해서(상품명 220px, 상품코드 170px 등) 해결.
- 브라우저 스크린샷으로 직접 재확인: 3단계 표 글자 안 겹침 정상.
- **아직 커밋 안 함.**

## 되돌림 + 진짜 원인 수정: 카테고리/채널 탭 동시 활성화 (Claude, 2026-09-16)

사용자 피드백: "이건 그전으로 돌려 난 이거에 대해 문제 제기 한적 없음. 그리고 제품카테고리 버튼이랑 채널별 버튼이랑 같이 중복으로 눌려 — 각각 독립된 버튼이니 중복으로 눌릴 이유가 없음."

- 탭 줄 레이아웃(위/아래 분리 + 가로 구분선)은 요청한 적 없는 변경이라 **원래대로(한 줄 + 세로 `|` 구분선) 되돌렸다.**
- 진짜 문제는 따로 있었다: 카테고리 탭 7개 중 첫 번째("아이소핑크")와 채널 탭 7개 중 첫 번째("한국단열")가 **처음부터 둘 다 active로 표시**돼 있었고, `setHkPricingTab()`/`setHkChannel()`이 서로 반대쪽 줄의 active 표시는 전혀 안 지웠다 — 그래서 카테고리를 아무리 바꿔도 채널 쪽 버튼 하나가 계속 파랗게 눌려 있고, 채널을 바꿔도 카테고리 쪽 버튼이 계속 눌려 있는 것처럼 보였다(실제로 지금 보고 있는 화면 모드와 안 맞는 버튼이 같이 강조돼 있던 것).
- 고침: 채널 탭은 처음에 아무것도 active로 시작하지 않게 하고, `setHkPricingTab()`은 채널 탭 쪽 active를 전부 지우고, `setHkChannel()`은 카테고리 탭 쪽 active를 전부 지우도록 해서 — **항상 정확히 하나(지금 보고 있는 화면에 해당하는 탭)만 active로 표시**되게 했다.
- 브라우저에서 검증: 초기 상태(카테고리만 active), 채널 클릭 시(채널만 active·카테고리 active 해제), 다시 카테고리 클릭 시(카테고리만 active·채널 active 해제) 전부 정상.
- **아직 커밋 안 함.**

## 3단계 표 칼럼 너비 조정 (Claude, 2026-09-16)

사용자 요청: 상품명이 더 길어질 수 있으니 다른 칼럼을 줄이고 상품명을 늘리고, 상품코드는 더 줄여도 되고, 우측 끝 "수정 전 판매가/가격차이/수정전 배송비" 3칸은 같은 폭으로.

`.hk-iso-listing-table`의 `<colgroup>` 조정: 상품명 220→300px, 상품코드 170→130px(길어지면 2줄로 자동 줄바꿈 — 이미 `word-break:break-all` 걸어둠), 마지막 3칸(수정 전 판매가/가격차이/수정전 배송비)을 전부 100px로 통일. 표 `min-width`도 1600px로 맞춤. 브라우저에서 칼럼 실제 폭 확인 완료.
- **아직 커밋 안 함.**

## 3단계 표 — 상품ID를 rowspan 병합 셀로 (Claude, 2026-09-16)

사용자 피드백: 상품ID가 "같은 상품"이라는 뜻인데 지금은(같은 상품의 첫 행에만 값 넣고 나머지는 빈칸) 그게 표현이 안 된다 — 빈칸으로 놔두면 엑셀의 병합 셀처럼 안 보이고 그냥 데이터가 빠진 것처럼 보임.

- 진짜 `<td rowspan="N">`으로 상품ID 셀을 그 상품의 옵션 행 수만큼 세로로 병합했다(엑셀에서 하던 셀 병합과 정확히 같은 방식) — 첫 행에만 셀을 만들고 나머지 행은 그 칼럼 자체를 생략(HTML rowspan 규칙).
- 추가로 상품(그룹)별로 행 배경을 흰색/연한 인디고로 번갈아 칠해서(`.hk-iso-listing-group-a`/`-b`) 병합 셀이 없어도 그룹 경계가 한눈에 보이게 했다 — 기본 표의 짝수행 줄무늬보다 우선하도록 선택자를 더 구체적으로 잡음.
- 상품ID 칼럼 자체도 인디고 배경 + 좌우 테두리로 강조해서 "이 칸이 여러 행에 걸쳐있다"가 시각적으로 분명하게 보이게 함.
- 브라우저에서 확인: 상품ID 셀 2개가 각각 정확히 12개 행(그 상품의 옵션 수)만큼 rowspan 됨, 스크린샷으로 그룹 경계도 뚜렷하게 보이는 것 확인.
- **아직 커밋 안 함.**

## 실사용 중 발견된 진짜 버그 + 마무리 다듬기 (Claude, 2026-09-16)

사용자가 실제 앱 화면 스크린샷을 보내서 "439103571랑 2229818356 경계가 없다"고 지적 — 확인해보니 두 가지가 겹쳐서 일어난 문제였다.

**진짜 버그**: `.hk-iso-listing-table`이 `.hk-iso-draft-table` 클래스를 같이 쓰는데, 그 공용 클래스에 `nth-child(3)`/`nth-child(6)`/`nth-child(15)`/`nth-child(16)` 위치 기준 스타일 규칙이 걸려있었다(원래 16칸짜리 기준단가/배송비 표의 특정 칼럼을 겨냥한 것). 그런데 3단계 표는 상품ID 칸에 rowspan을 쓰기 때문에, 같은 상품의 2번째 행부터는 그 칸 자체가 없어서 **뒤 칸들의 nth-child 순번이 전부 한 칸씩 밀렸다** — 그 결과 "네이버판매가"·"재고수량" 같은 엉뚱한 칸에 원래 다른 칼럼(판매사이즈 등)을 위한 파란 글씨/배경이 덧씌워져서 표가 지저분해 보였고, 그 시각적 잡음 때문에 상품ID 그룹 경계(연한 배경 줄무늬만 있던 상태)가 더 안 보였다. `:not(.hk-iso-listing-table)`로 3단계 표를 그 규칙들에서 빼서 해결.
- 그룹 경계는 배경 줄무늬 대신 새 상품 시작 행에 굵은 보라색 윗선(`hk-iso-listing-group-start`)을 긋는 방식으로 바꿨다 — 사용자가 "이렇게 칸 나누면 될 것 같고 배경색 구분은 안 해도 될 것 같다"고 확인해줘서, 배경 줄무늬 코드는 다시 지웠다(굵은 윗선만 남김).
- 표 안 모든 칼럼을 가운데 정렬로 통일해달라는 요청도 반영(원래 상품명만 왼쪽 정렬이었음, 이 표에서만 가운데로 되돌림 — 다른 1·2단계 표는 그대로 왼쪽 유지).
- 브라우저에서 재검증: nth-child 오염 사라짐(재고수량 칸이 다시 기본 스타일로), 상품ID 경계에 굵은 선 정확히 표시, 상품명까지 전부 가운데 정렬 확인.
- **아직 커밋 안 함.**

## 상품ID → 스마트스토어 링크 (Claude, 2026-09-16)

3단계 표의 상품ID(rowspan 병합 셀)를 클릭하면 `https://smartstore.naver.com/hkdy/products/{상품번호}`로 새 탭에서 열리게 했다(`target="_blank" rel="noopener noreferrer"`). 인디고 밑줄 링크 스타일로 표시. 브라우저에서 두 상품ID(439103571, 2229818356) 모두 정확한 URL로 링크가 걸리는 것 확인.
- **아직 커밋 안 함.**

## 접착식 600x900 2단계 데이터 재확인 (Claude, 2026-09-16, 변경 없음)

사용자가 "접착식 압출법 특호 35kg--600*900"(상품번호 439103571 // 2229818356 // 3020442618) 2단계 원본 엑셀 5행을 다시 보내줘서 대조했다 — `adhesive_600x900_1` 블록(지난번에 원본 엑셀에서 직접 읽어 넣어둔 값)과 5행 전부(10T~50T의 첫 변형 행: 실제배송비/배송비차액/1장당차액/배송비플러스금액/실판매가) **정확히 일치**해서 코드 변경 없음. 브라우저에서 재검증 완료.

## 상품번호 439904706 추가 (Claude, 2026-09-16)

사용자가 상품번호 439904706의 "압출법 특호"·"접착식 압출법 특호" 2단계 표를 줬는데, 값을 대조해보니 **기존 `iso_600x900_1`·`adhesive_600x900_1` 블록의 데이터와 전부 정확히 일치**했다(같은 배송비 정책을 쓰는 상품이라는 뜻). 그래서 새 블록을 따로 안 만들고 두 블록의 `productNumbers` 배열에 `'439904706'`만 추가했다 — 데이터 중복 없이 "이 상품번호도 같은 기준단가/배송비를 쓴다"는 걸 표현하는 원래 설계(배열로 여러 상품번호를 한 블록에 묶는 방식) 그대로 따른 것. 3단계(실제 등록 상품) 자료는 이 상품번호에 대해 아직 안 받아서 손대지 않음.

## 900x1800 계열 2단계 — 배송비 구조가 아예 다른 케이스 추가 (Claude, 2026-09-16)

사용자가 "압출법 특호 35kg--900*1800" 2단계 데이터 2블록(유료배송 기본 + 상품번호 5695312387 무료배송)을 줬다. 값 대조 결과, 사이즈/수량 패턴(코드 뒤 숫자: 10_1,20_1... vs 10_10,20_5...)으로 첫 블록은 `iso_900x1800_single`, 둘째 블록은 `iso_900x1800_bundle`(묶음)에 대응한다는 걸 확인(1단계 판매가와도 정확히 일치).

**600x900류와 근본적으로 다른 점 2가지**:
1. 열 구성 자체가 다르다 — "5장당" 기준이 아니라 "1장당 배송비 + 몇 개마다 배송비를 청구하는지(실제배송비가 숫자가 아니라 "5개마다" 같은 텍스트 규칙)" + "알림 쿠폰" 구조.
2. **실판매가 공식이 무료배송 여부에 따라 완전히 다르다**: 유료배송(첫 블록)은 `실판매가 = 판매가` 그대로(배송비 플러스 금액은 참고용, 실제로는 결제 시 배송비를 별도로 받아서 안 얹음) — 반면 무료배송(둘째 블록, 상품번호 "(무료)")은 `실판매가 = 판매가 + 배송비 플러스 금액`(무료배송으로 보이게 배송비를 가격에 얹음). 이건 세션 초반 원본 엑셀을 처음 봤을 때 발견했던 바로 그 문제 — 이번에 처음으로 실제 구현.

**구현**:
- 블록에 `schema` 필드 추가(`'per5'` = 기존 600x900류, `'per1Coupon'` = 이번 900x1800류) + `isFreeShipping` 플래그.
- 새 렌더러 `_hkIsoShippingBlockHtmlPerUnit()` / 재계산 `recalcHkIsoShippingRowPerUnit()` — 무료배송이면 배송비 칸에 "배송비 무료" 라벨만 뜨고 실제배송비는 숫자 입력칸, 유료배송이면 1장당배송비 입력칸 + 실제배송비는 텍스트 입력칸(regex 몇개마다).
- `_hkIsoLookupFinalPriceByCode()`(3단계 VLOOKUP)도 schema별로 플러스금액 적용 여부를 다르게 계산하도록 수정.
- **상품코드 오타 그대로 보존**: 70T·100T 무료배송 옵션의 실제 등록 코드가 `IIso_900_1800_70_1`(I가 두 번)인 걸 원본 엑셀에서도 확인했었는데, 이번에도 똑같이 와서 그대로(고치지 않고) 반영 — `codeOverride` 필드로 자동 생성 코드 대신 이 오타 코드를 쓰게 했다. VLOOKUP 함수도 이 오타 코드로 정확히 찾아지는 것까지 확인.
- 브라우저에서 14행(단품 7 + 묶음 7) 전부 실판매가 검증 — 엑셀과 정확히 일치(단품은 판매가 그대로, 묶음은 판매가+플러스금액).
- **아직 커밋 안 함.**

## 상품ID 중앙정렬 진짜 원인 + 재고수량 하드코딩 제거 (Claude, 2026-09-16)

**중앙정렬 재확인**: 사용자가 "상품ID 중앙정렬 안 되어 있다"고 지적해서 `getComputedStyle`로 직접 찍어봤더니 실제로 `text-align: right`였다. 원인은 `.pricing-table tbody td { text-align: right }`(기본 표 공통 규칙, 클래스 1개+타입 2개)가 `.hk-iso-draft-table td { text-align: center }`(클래스 1개+타입 1개)보다 명시도가 높아서 항상 이기고 있었던 것 — 지난번에 상품명만 센터로 보였던 건 그 선택자가 클래스 2개(`.hk-iso-listing-table .hk-iso-draft-name`)라 우연히 명시도가 더 높아서 먹혔던 거였고, 나머지 칸(상품ID 포함)은 계속 오른쪽 정렬 상태였다. `.hk-iso-listing-table.pricing-table tbody td { text-align:center }`(클래스 2개+타입 2개)로 확실히 이기도록 고쳐서 표 전체 14칸 전부 `getComputedStyle` 기준 `center`로 확인.

**재고수량 하드코딩 제거**: 사용자가 "내부 숫자가 그냥 보여주기용인지 실제 데이터인지 확인해달라"고 해서 표 전체를 다시 훑었다 — "재고수량" 칸만 `<td>99,999,999</td>`로 템플릿에 문자열 그대로 박혀있고 `HK_ISO_CHANNEL_LISTINGS` 데이터와 전혀 연결이 안 돼 있었다(진짜 "보여주기용 숫자"였음). `item.stock ?? 99999999`로 바꿔서 실제 데이터 필드가 될 수 있게 했다 — 지금 예시는 전부 재고 사실상 무제한이라 기본값 그대로 쓰지만, 앞으로 특정 옵션이 품절/한정수량이면 그 항목에 `stock: N`만 추가하면 반영됨. 나머지 칸(배송비/배송비기준/제주배송비/편도교환/수정전판매가/수정전배송비)은 전부 `product`/`item` 데이터에서 오는 걸 재확인 — 하드코딩된 건 재고수량 하나뿐이었음.
- **아직 커밋 안 함.**

## 900x1800/600x900 고티 2단계 나머지 전부 반영 (Claude, 2026-09-16)

사용자가 "알림쿠폰이랑 배송비 플러스 사이 빈 열은 그냥 빈 걸로 두고 채워라"고 해서, per1Coupon 스키마 표에 "1장당배송비차액"(원본 엑셀에도 항상 빈칸) 칼럼을 추가로 넣었다(헤더+`—` 빈 셀). 그 직후 사용자가 남은 2단계 원본 엑셀 데이터를 통째로 줬다(9개 표). 하나씩 값 대조해서 기존 블록 확장 3건 + 신규 블록 4건으로 정리했다.

**기존 블록에 추가만 한 것(데이터가 이미 있던 것과 완전히 일치)**:
- `iso_600x900_1` productNumbers에 `442086644` 추가.
- `adhesive_600x900_1` productNumbers에 `3736232926` 추가, 그리고 이전에 "실측 전이라 자리만 채워둔" 묶음(2번째) 행 5개(10T-10장/20T-5장/30T-3장/40T-2장/50T-2장)를 "쿠팡전용" 표의 실측값으로 교체 — 이 5행은 스마트스토어 상품번호가 아니라 별도 채널("쿠팡전용", 상품번호 없이 채널명으로만 구분)이 쓴다는 것도 주석으로 남겨둠.

**공통 원가/기준단가 구조 일반화**: 600 계열 고티 표가 "5장당"이 아니라 "배송비 무료(0원 기준) + 1장 단위" 구조라 기존 `per5` 렌더러/재계산 함수에 `divisor`(기본 5) 옵션을 추가해서 재사용했다(하드코딩된 ÷5를 ÷divisor로 바꿈, divisor===1이면 헤더도 "배송비 무료"/"실제배송비"로 자동 전환).

**신규 블록 4개**(전부 schema:'per1Coupon', 유료/무료배송 구분 반영):
- `adhesive_900x1800_single_1` — 접착식 900x1800 단품, 유료배송(상품번호 없음, 기본값).
- `adhesive_900x1800_bundle_1` — 접착식 900x1800 묶음, 무료배송(상품번호 5695312387).
- `iso_900x1800_high_t_1` — 900x1800 고티, 무료배송(상품번호 5697937041).
- `iso_600x900_high_t_1` — 600 계열 고티, 무료배송(상품번호 4995022274) — `divisor:1`, `baseShipping5:0`으로 일반화된 per5 렌더러 사용, 배송비 플러스 금액이 원본에 전부 빈 칸이라 0으로 둠(무료배송인데 아직 가격에 안 얹은 상태로 보임).

이걸로 아이소핑크 카테고리의 사이즈/판매방식 조합 9개 중 8개(쿠팡 위너 전체만 남음)에 2단계 데이터가 다 들어갔다.

브라우저에서 전체 검증: 8개 블록 총 69행 전부 엑셀 원본 실판매가와 정확히 일치(신규 블록 포함, divisor=1 일반화 케이스 포함). "1장당배송비차액" 빈 칸 표시도 정상, 상품번호 없는 블록은 "상품번호 미지정(기본값)" 안내 정상 표시.
- **아직 커밋 안 함.**

## 쿠팡 위너 2단계 — 마지막 남은 조합까지 전부 완료 (Claude, 2026-09-16)

사용자가 "쿠팡 위너 전용" 표(26행, 상품코드 접두어 `IsoC_`)를 줬다. 이걸로 아이소핑크 9개 사이즈/판매방식 조합 전부에 2단계가 들어갔다. 이번 건은 지금까지 중 가장 예외가 많아서 `per5` 렌더러/계산 함수를 두 번 더 일반화했다.

1. **상품코드 접두어 확장**: `_hkIsoDraftProductCode()`에 `codePrefix` 인자를 추가해서 `Iso_`/`IsoA_` 외에 `IsoC_`(쿠팡 위너)도 만들 수 있게 함 — `_hkIsoLookupFinalPriceByCode()`도 같이 반영.
2. **행마다 기준배송비가 다름**: 이 표는 수량이 많은 행(10장/20장)일수록 기준배송비도 배수로 올라간다(6000원 기준 × 묶음수, 예: 10장→12000, 20장→24000). 블록 전체에 고정값 하나만 쓰던 `baseShipping5`를 행별로 덮어쓸 수 있게(`ship.baseShipping`) 확장.
3. **"5장당" 개념이 아예 없는 행**: 600*860/600*430/900*1800 규격(큰 두께)은 원본 엑셀에도 기준배송비·배송비차액 칸이 통째로 비어있다 — `baseShipping: null`이면 그 계산 자체를 건너뛰고 화면에도 "—"만 표시하도록 `hasBase` 분기를 추가(재계산 함수도 같이 대응).
4. **"1장당배송비차액"이 수량마다 다르게 나뉨**: 대부분 행은 배송비차액을 그 행의 실제 판매수량으로 나누면 원본 값과 맞아떨어져서(`perRowDivisor:true`, 판매사이즈 뒤 숫자로 나눔) 그렇게 계산했다. 단, 40T-5장·50T-2장 두 행은 원본 엑셀 자체에서 이 계산과 안 맞는 값이 적혀있었다(같은 수량인 다른 행들과 비교해도 일관성이 없어 원본 입력 실수로 보임) — 이 칸은 참고용 표시일 뿐 실판매가 계산에는 전혀 쓰이지 않으므로, 정확한 실판매가를 지키는 쪽을 택하고 이 두 칸만 원본과 다르게 보일 수 있음을 주석으로 남겼다.

브라우저에서 26행 전부(상품코드 포함) 검증 — 실판매가 전부 정확히 일치, "5장당" 개념이 없는 행은 기준배송비/배송비차액/1장당차액 칸에 "—"가 제대로 뜨고 실제배송비·플러스금액·실판매가는 정상 표시되는 것 확인.

이걸로 **한국단열 아이소핑크 2단계(배송비/실판매가) 작업이 사용자가 준 원본 엑셀 기준으로 전부 끝났다** — 9개 조합(일반 600x900/600 계열 고티/900x1800 단품·묶음·고티, 접착식 600x900/900x1800 단품·묶음, 쿠팡 위너) 모두 반영됨. 3단계(채널별 실제 등록 상품)는 "한국단열" 채널의 상품번호 439103571/2229818356만 들어가 있고 나머지 상품번호·채널은 아직임.
- **아직 커밋 안 함.**
