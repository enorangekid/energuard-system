# 작업현황 — 한국단열 단가표 (최종 업데이트: 2026-09-16)

> 사용자가 휴가 들어가기 전 마지막 메모.

## ⭐ 다음에 할 일 (딱 하나 남음): 3단계 "매칭" 마무리

**한국단열 아이소핑크 3단계(채널별 실제 등록 상품)**가 아직 다 안 채워졌다.

- 지금 있는 것: `HK_ISO_CHANNEL_LISTINGS.hkd`(한국단열 채널)에 상품번호 **439103571**, **2229818356** 두 개만 들어있음(각 12개 옵션, 일반 8 + 접착식 4, 10T~40T만).
- **부족한 것**:
  1. 같은 "한국단열" 채널 안에서도 **나머지 상품번호**(2단계에는 이미 반영된 `3020442618`, `439904706`, `3736232926`, "쿠팡전용", `5695312387`(무료), `5697937041`(무료), `4995022274`(무료), `442086644` 등)의 **3단계 실제 등록 데이터**(상품ID, 옵션별 "수정 전 판매가" — 실제 스토어에 지금 올라가 있는 값)를 아직 안 받음.
  2. **채널 자체**도 "한국단열" 하나만 채워져 있고 나머지 6개(한국단열라이프/홈페이지/쿠팡/쿠팡_부자재/ESM/11번가)는 전부 "아직 입력 전" 상태.
  3. 아이소핑크 말고 다른 6개 카테고리(스티로폼/열반사단열재/단열벽지/창문형단열재/기타단열재/부자재)는 1~3단계 전부 아직 손 안 댐 — 해당 원본 엑셀 자료 받으면 시작.

**이어서 할 때 방법**: 사용자가 엑셀에서 "상품명/상품코드/상품ID/네이버판매가/기준가/옵션추가금/재고수량/배송비/배송비기준/제주배송비/편도교환/수정 전 판매가/가격차이/수정전 배송비" 표를 캡처해서 주면, `js/pricing-hankook.js`의 `HK_ISO_CHANNEL_LISTINGS`에 그 채널·상품번호 블록을 추가하면 된다(패턴은 이미 있는 `hkd` 채널 예시 그대로 따라하면 됨 — `product.items[].prevPrice`만 실제 값으로 채우면 네이버판매가/기준가/옵션추가금/가격차이는 전부 자동 계산됨).

## 2단계는 다 끝났음 (더 안 건드려도 됨)

아이소핑크 9개 사이즈/판매방식 조합(일반 600x900·600 계열 고티·900x1800 단품/묶음/고티, 접착식 600x900·900x1800 단품/묶음, 쿠팡 위너) 전부 원본 엑셀 실측값 반영 완료, 코드 매칭 전수 검사(중복 0건·미해결 0건)까지 확인함. 아래 "900x1800/600x900 고티 2단계 나머지 전부 반영"·"쿠팡 위너 2단계" 절 참고.

## 현재 Git 상태

- 수정: `js/pricing-hankook.js`
- 수정: `css/pricing.css`
- 수정: `index.html`
- 수정: `js/pricing.js`
- 신규: `작업현황.md`
- 마지막 검증: `node --check js/pricing-hankook.js`, `node --check js/pricing.js`, `git diff --check` 통과

## 현재 구현 완료 사항

### 1. 한국단열 아이소핑크 새 단가표

`js/pricing-hankook.js`에 다음 9개 기준단가 그룹이 모두 데이터와 계산식까지 연결돼 있다.

| 내부 키 | 현재 화면명 | 행 수 |
|---|---|---:|
| `iso_600x900` | 아이소핑크 600x900 | 14 |
| `adhesive_600x900_a` | 접착식 아이소핑크 600x900 | 10 |
| `iso_900x1800_bundle` | 아이소핑크 900x1800_묶음 | 7 |
| `iso_900x1800_single` | 아이소핑크 900x1800_단품 | 7 |
| `iso_900x1800_high_t` | 아이소핑크 900x1800_고티 | 4 |
| `adhesive_900x1800_single` | 접착식 아이소핑크 900x1800_단품 | 5 |
| `adhesive_900x1800_bundle` | 접착식 아이소핑크 900x1800_묶음 | 5 |
| `iso_600x900_high_t` | 아이소핑크 600x900_고티 | 5 |
| `iso_coupang_winner` | 아이소핑크_쿠팡 위너 | 26 |

~~현재 화면은 위 9개가 각각 가로 하위 탭으로 노출된다.~~ → **(완료) 3개 상위 탭(일반/접착식/쿠팡 위너) + 아코디언 구조로 변경됨. 맨 아래 절 참고.**

### 2. 공통 원가 구조

에너가드 아이소핑크 방식처럼 `두께구간 기본 원가 + 두께별 추가마진`으로 변경했다.

기본 원가:

- 10T~20T: 260원/mm
- 30T~180T: 198원/mm
- 185T 이상: 201원/mm

추가마진 기본값:

| 두께 | 추가마진 | 적용 원가 |
|---:|---:|---:|
| 10T | -10 | 250 |
| 20T | -10 | 250 |
| 30T | +27 | 225 |
| 40T | +27 | 225 |
| 50T | +22 | 220 |
| 70T | +22 | 220 |
| 100T | +22 | 220 |
| 250T | +29 | 230 |
| 500T | +29 | 230 |

- 공통 원가를 바꾸면 모든 기준단가 그룹이 함께 다시 계산된다.
- 접착식은 계산된 원장 원가에 원장당 가공비 1,350원을 추가한다.
- 마진 편집은 펼침 영역이 아니라 에너가드와 같은 별도 팝업이다.
- 팝업 HTML은 `index.html`의 `#hkIsoMarginModal`에 추가했다.
- 마진 편집 버튼은 접착 가공비 입력란 바로 옆, 원가표 상단 왼쪽에 배치했다.

### 3. 판매가 표 기능

- 참고마진을 기준으로 예상판매가를 100원 단위로 자동 계산한다.
- 최종 판매가는 고정 표시되며 `판매가 편집` 버튼을 눌러야 수정할 수 있다.
- 판매원가, 마진, 판매수수료 6%, 부가세 10%, 배송비, 장당마진, 순수마진율을 계산한다.
- 고티 행은 배송비도 예상판매가와 순수마진 계산에서 차감한다.
- 계산 셀에 마우스를 올리면 산출식을 `title` 툴팁으로 표시한다.
- 원본 표의 보조 `판매가/원장 판매가` 열은 데이터 보존 목적으로 남기되 불투명도를 낮췄다.
- `600x430`은 원장 6분할, `600x860`·`600x900`은 3분할, `430x430`은 8분할, `900x1800`은 1분할로 계산한다.

### 4. 화면 진입과 정리

- 한국단열로 진입할 때 아이소핑크 첫 화면으로 초기화하도록 `js/pricing.js`의 `setPricingCompany('hkd')`에서 `resetHkPricingView()`를 호출한다.
- 숨겨 보관하던 구형 한국단열 아이소핑크 렌더러, 시드 데이터, 구형 계산 함수와 전용 CSS를 모두 삭제했다.
- 현재 가격과 마진 데이터는 로컬 자바스크립트 상수/화면 상태에만 있다. 새로고침하면 초기값으로 돌아가며 Supabase 저장은 아직 없다.

## (완료) 3개 상위 탭 + 아코디언 — 원래 계획

사용자 최종 요청은 기존 9개 가로 탭을 다음 **3개 상위 탭**으로 합치는 것이었다. 아래는 원래 계획이고, 실제 구현 내용은 이 문서 맨 아래 "3개 상위 탭 + 아코디언 구현 완료 (Claude)" 절에 있다.

### 일반 아이소핑크

아코디언 섹션:

1. 600x900 일반 — `iso_600x900`
2. 600 계열 고티(실제 판매사이즈 600x860·600x430) — `iso_600x900_high_t`
3. 900x1800 단품 — `iso_900x1800_single`
4. 900x1800 묶음 — `iso_900x1800_bundle`
5. 900x1800 고티 — `iso_900x1800_high_t`

### 접착식 아이소핑크

아코디언 섹션:

1. 600x900 — `adhesive_600x900_a`
2. 900x1800 단품 — `adhesive_900x1800_single`
3. 900x1800 묶음 — `adhesive_900x1800_bundle`

### 쿠팡 위너

아코디언 섹션:

1. 쿠팡 위너 전체 — `iso_coupang_winner`

구현 원칙:

- 한 상위 탭 안에서 아코디언 하나만 펼친다.
- 상위 탭을 바꾸면 해당 탭의 첫 아코디언을 기본으로 연다.
- 아코디언 머리에는 `기준단가 N개`를 표시한다.
- `판매가 편집`은 현재 펼친 아코디언 안의 판매가만 편집 가능하게 한다.
- `HK_ISO_CONNECTED_DRAFTS`의 기존 내부 키는 이후 상품 매핑의 기준키로 사용해야 하므로 변경하지 않는다.
- 현재 9개 표 데이터와 계산 함수는 그대로 재사용하고 렌더 구조만 바꾼다.

## 옵션 중복 확인 결과

쿠팡 위너를 제외하고 `상품 종류 + 두께 + 판매사이즈 + 묶음수량`을 옵션 기준으로 보면 일반 아이소핑크에 중복 2개가 있다.

| 기준단가 그룹 | 옵션 | 최종 판매가 |
|---|---|---:|
| 900x1800 묶음 | 70T · 900x1800-1 | 26,500원 |
| 900x1800 단품 | 70T · 900x1800-1 | 27,000원 |
| 900x1800 묶음 | 100T · 900x1800-1 | 35,500원 |
| 900x1800 단품 | 100T · 900x1800-1 | 36,500원 |

나머지는 일반/접착식 구분을 포함하면 중복이 없다. 실제 상품과 연결하기 전에 70T·100T 1장 상품을 단품과 묶음 중 어느 기준으로 연결할지 사용자가 결정해야 한다. 같은 상품번호·옵션에 두 기준단가를 동시에 연결하면 안 된다.

## 이후 실제 상품 연결 계획

현재 표는 실제 스토어 판매가가 아니라 **상품 가격을 만들기 위한 기준단가표**다. 화면 그룹과 상품 매핑을 분리한다.

1. 각 기준단가 행에 안정적인 `price_key`를 부여한다.
   - 예: `iso_900x1800_single__70__900x1800-1`
   - 구성: `기준단가 그룹 키 + 두께 + 판매사이즈/묶음수량`
2. 실제 상품 매핑에는 최소한 다음 정보를 저장한다.
   - `price_key`
   - 상품번호
   - 옵션 ID 또는 옵션명
   - 판매 채널/스토어
   - 활성 여부
3. 화면 이름이나 아코디언 배치를 바꿔도 `price_key`는 바꾸지 않는다.
4. 실제 상품 가격은 기준단가와 별도 값으로 수집한다.
5. 이후 검사 화면에서 `기준단가 / 실제 판매가 / 차액 / 일치 여부`를 비교한다.
6. 아코디언 머리에는 향후 `연결상품 N개`, `가격검사 정상/불일치` 요약을 추가할 수 있다.

저장 방식은 아직 결정하지 않았다. 상품 연결을 시작하기 전에 한국단열 기준단가와 매핑을 저장할 Supabase 테이블 구조를 정해야 한다.

## 기존 스토어 가격검사 관련 참고

- 스토어 가격검사와 경쟁사 가격 확인 기능은 별도로 구현돼 있다.
- 실제 로드되는 확장프로그램은 별도 레포 `../Naver-rank/shopping-rank-extension/`이다.
- 통합 빌드/복사는 `node tools/integrate-price-test.cjs`를 사용한다.
- 한국단열 상품 연결 후 기존 `getTablePrice`, 옵션 매핑, 가격검사 구조를 재사용할 수 있다.

## 3개 상위 탭 + 아코디언 구현 완료 (Claude, 2026-09-16)

`js/pricing-hankook.js`에서 기존 9개 가로 하위 탭(`HK_ISO_DRAFT_TABS`)을 삭제하고, 3개 상위 탭 안에 아코디언을 두는 구조(`HK_ISO_SUPER_TABS`)로 교체했다. `HK_ISO_CONNECTED_DRAFTS`의 키(내부 id)는 계획대로 손대지 않았다.

- **상위 탭 3개**: 일반 아이소핑크 / 접착식 아이소핑크 / 쿠팡 위너 — `#hkIsoSuperTabBar`에서 `setHkIsoSuperTab(superId, el)`로 전환.
- **아코디언**: 각 상위 탭 안에 계획대로 섹션 배치(일반 5개, 접착식 3개, 쿠팡 위너 1개). 헤더에 `기준단가 N개`(= 해당 기준단가표의 행 수) 표시. `toggleHkIsoAccordion(accId)`로 그 아코디언 하나만 독립적으로 열고 닫는다 — 라디오 방식이 아니라 진짜 토글이라 여러 개를 동시에 펼 수도, 다 접을 수도 있다(2026-09-16 사용자 피드백으로 라디오 방식에서 변경).
- **상위 탭 전환 시**: 아코디언 상태를 건드리지 않는다 — 탭을 오갈 때 각 탭에서 열어뒀던 아코디언 상태가 그대로 유지된다.
- **`resetHkPricingView()`만 예외적으로 강제 초기화**: 한국단열에 새로 진입할 때만 첫 상위 탭(일반)의 첫 아코디언(600x900 일반)만 열고 나머지는 닫은 깨끗한 상태로 되돌린다 — "첫 진입 시 맨 앞이 보여야 한다"는 기존 요구사항은 그대로 유지.
- **판매가 편집 범위**: `_setHkIsoFinalPriceEditing()`가 `.hk-iso-super-pane.active .hk-iso-accordion.open` 안의 입력칸만 활성화한다 — 다른 아코디언/탭의 판매가는 편집 버튼을 켜도 여전히 잠겨 있다. 상위 탭이나 아코디언을 바꾸면 편집 모드는 자동으로 꺼진다.
- **`resetHkPricingView()`**: 한국단열 진입 시 항상 첫 상위 탭(일반 아이소핑크)의 첫 아코디언(600x900 일반)을 연다 — `js/pricing.js`의 `setPricingCompany('hkd')`에서 계속 호출.
- 표 자체(`_hkIsoDraftSalesTable`, 계산 로직, 마진 팝업 등)는 전혀 건드리지 않았다 — 바깥 탭/아코디언 껍데기만 바꿨다.
- CSS: `css/pricing.css`에서 안 쓰이게 된 `.hk-iso-draft-tabs`/`.hk-iso-draft-pane`을 지우고 `.hk-iso-super-pane`/`.hk-iso-accordion*`로 교체.
- 로컬 정적 서버(python http.server)로 실제 렌더 확인 완료: 상위 탭 전환, 아코디언 단일 열림, 판매가 편집 범위 제한(개발자 콘솔로 활성화된 입력칸 개수 직접 카운트해서 검증)까지 정상 동작.
- **아직 커밋 안 함.**

### 남은 것 (변경 없음, MD 원안 그대로 유효)

- 옵션 중복 확인 결과(70T·100T 900x1800 단품 vs 묶음) — 사용자 결정 필요, 손대지 않음.
- 실제 Supabase 매핑 테이블 연결 — 착수 전.

## 상품코드 열 추가 (Claude, 2026-09-16)

사용자가 실제 스마트스토어 등록 상품코드 목록(`Iso_430_430_10_3` 형태)을 주고 "1단계 표에서 완성하고 그 값을 네이버 상품코드와 연동"하기로 방향을 정했다. 2단계(배송비 보정)·3단계(네이버 옵션추가금) 표를 따로 만들지 않고, 1단계 기준단가표 안에서 전부 끝내는 방향.

- `_hkIsoDraftProductCode(saleSize, thickness, isAdhesive)`를 추가해 판매사이즈·두께·수량으로 코드를 기계적으로 생성한다: `(일반)Iso_ / (접착식)IsoA_` + 규격(`430_430` 등) + 두께 + 수량. 예: 10T·430×430-3장 → `Iso_430_430_10_3`.
- `_hkIsoDraftSalesTable()`의 "판매사이즈" 열 바로 뒤에 "상품코드" 열을 추가했다(쿠팡 위너 제외 — 이 코드 체계를 안 쓰는 걸로 보여서 뺐음, 확인 필요하면 사용자에게 물어볼 것). `<tr data-product-code="...">`로도 심어둬서 나중에 실제 상품 매핑(Supabase)의 키로 그대로 쓸 수 있게 해뒀다.
- 사용자가 준 코드 50개(중복 제거)를 브라우저에서 직접 대조해서 **전부 정확히 일치**하는 것 확인했다(콘솔에서 `missingFromActual: []`). 생성된 코드 중 사용자 목록에 없던 5개(`IsoA_600_900_10_10` 등)는 접착식 600x900의 두 번째 수량 변형 행(엑셀엔 있지만 아직 코드 목록엔 안 올라온 것으로 보임) — 버그 아님, 사용자 확인 필요하면 물어볼 것.
- **아직 커밋 안 함.** 이 문서 상단의 "다음 작업자"가 이어서 진행하면 된다.

## 2단계(상품번호별 배송비/실판매가) 착수 — 원가 3단계 구조 확정 (Claude, 2026-09-16)

사용자가 원본 엑셀 `★단가표수정\한국단열\나눔\아이소핑크_실제단가.xlsx`(시트: 수정단가)를 확인해보라고 해서 열어봤고, **1단계 표 안에서 2단계를 자동 흡수하는 건 불가능하다는 걸 확인**했다(이전에 내가 "2단계 표 필요없다"고 제안했던 게 틀렸음 — 정정함).

### 왜 안 되는지 (엑셀에서 확인한 사실)
- 같은 옵션(예: `Iso_430_430_70_1`)이 **서로 다른 상품번호 블록에 각각 따로** 들어있다 — 상품번호마다 실제배송비가 독립적으로 존재.
- **무료배송 여부에 따라 계산 공식 자체가 다르다**: 일반 상품은 `실판매가 = 판매가`(배송비 별도 청구), 무료배송 상품(예: 상품번호 `5695312387(무료)`)은 `실판매가 = 판매가 + 배송비플러스금액`.
- 900×1800 단품 블록은 아예 열 구성이 다르다(`1장당 배송비` / `배송비 무료` / `알림 쿠폰`), "실제배송비" 칸에 숫자 대신 "5개마다"류 규칙 텍스트가 들어있기도 함.
- 쿠팡 위너는 `IsoC_` 접두어를 따로 쓴다(이전에 "코드 없음"이라 판단한 게 오류였음 — 정정함, 아직 미구현).
- 코드 자체에 오타로 보이는 게 실사용 데이터에 있음: `IIso_900_1800_70_1` / `IIso_900_1800_100_1`(I가 두 번) — 사용자에게 확인 필요.

### 확정한 3단계 배치 방향 (사용자와 논의해서 결정)
1단계·3단계는 자주 보니 탭으로 바로 전환, 2단계는 자주 안 보니 버튼 눌러서 여는 섹션(현재는 같은 페이지 안에서 펼치는 방식으로 구현, 필요하면 나중에 탭으로 승격 가능).

### 지금까지 구현한 것 (`js/pricing-hankook.js`)
- **1단계 표의 상품코드 컬럼은 도로 제거함**(사용자 지시) — `_hkIsoDraftProductCode()` 헬퍼 함수 자체는 남겨두고 2단계에서 쓴다.
- **2단계 첫 번째 예시 구현**: `HK_ISO_SHIPPING_BLOCKS` 배열에 "아이소핑크 600x900"(상품번호 439103571 // 2229818356 // 3020442618) 블록 하나를 실제 엑셀 값 그대로 넣었다. 계산 관계:
  - `배송비차액 = 5장당실제배송비 − 5장당배송비(기준)`
  - `1장당배송비차액 = 배송비차액 ÷ 5`(참고용 — 실제 판매수량과 무관하게 항상 5로 나눔, 엑셀 역산으로 확인)
  - `배송비 플러스 금액`은 위 참고값을 보고 **사람이 100원 단위로 직접 정하는 값**이라 순수 계산으로 못 만든다 — 그래서 노란 배경의 직접입력 칸(`hk-iso-ship-plus-input`)으로 뒀다.
  - `실판매가 = 1단계 판매가 + 배송비 플러스 금액`
- 렌더: `_hkIsoShippingBlockHtml(block)`가 블록 하나를 표로 그리고, `recalcHkIsoShippingRow(input)`이 입력값 바뀔 때마다 배송비차액/1장당차액/실판매가를 다시 계산한다.
- UI: 아이소핑크 결과 카드 헤더에 "배송비·실판매가(2단계)" 버튼을 추가했고, 누르면 아래에 `#hkIsoShippingSection`이 펼쳐진다(기본은 숨김, `toggleHkIsoShippingSection()`).
- **검증 완료**: 브라우저에서 직접 렌더해서 14행 전부(`basePrice`/`배송비차액`/`1장당차액`/`실판매가`) 엑셀 원본과 정확히 일치하는 것 확인(`Iso_430_430_10_3`→2,400원 등 전부 일치).
- **아직 안 한 것**: 접착식 600x900 블록, 900x1800(단품/묶음/고티) 블록들, 무료배송 케이스(다른 공식), 쿠팡 위너(`IsoC_` 코드) 블록 — 전부 사용자가 다음 예시를 줄 때마다 `HK_ISO_SHIPPING_BLOCKS`에 블록을 추가하는 식으로 확장하면 됨. 3단계(실제 네이버 옵션 등록 확인)는 아직 손 안 댐.
- **아직 커밋 안 함.**

### 버그 수정: 2단계 판매가가 1단계와 진짜로 연동되지 않던 문제 (Claude, 2026-09-16)

사용자가 "2단계 판매가 데이터 1단계에서 받고 있는거 맞아?"라고 물어봐서 확인했더니 — **아니었다.** `_hkIsoShippingBlockHtml()`이 `HK_ISO_CONNECTED_DRAFTS[...].rows`(정적 시드 배열)의 `row.price`를 읽고 있었는데, 이건 1단계 화면의 `.hk-iso-final-price-input`(실제 입력칸, "판매가 편집"으로 사람이 바꿀 수 있는 값)과는 별개의 값이었다. 페이지를 막 열었을 때는 둘 다 같은 시드값이라 우연히 일치해 보였을 뿐, 1단계에서 판매가를 실제로 편집하면 2단계는 그 변경을 전혀 반영하지 못했다.

고쳤다:
- `_hkIsoShippingBlockHtml()`이 이제 `#hkIsoAcc-{accordionId} .hk-iso-final-price-input`을 순서대로 읽어서 그 값을 basePrice로 쓴다(DOM이 없는 최초 렌더 시점엔 시드값으로 방어적 대체).
- 2단계 섹션은 이제 페이지 로드 시 미리 그리지 않고, **"배송비·실판매가(2단계)" 버튼을 눌러 열 때마다 새로 그린다** — 그 시점의 1단계 값을 그대로 가져오기 위해.
- 혹시 2단계를 열어둔 채로 1단계 값을 바꾸는 경우를 위해, 각 블록 헤더에 "1단계 판매가 새로고침" 버튼도 추가했다.
- 브라우저에서 직접 검증: 1단계 10T·430×430-3 판매가를 9,999원으로 바꾸고 2단계를 다시 열었더니 판매가 9,999원, 실판매가 9,799원(=9,999−200)으로 정확히 반영됨을 확인.
- **아직 커밋 안 함.**

## 채널 선택 탭 추가 (Claude, 2026-09-16)

한국단열 카테고리 탭(아이소핑크|스티로폼|...|부자재) 옆에 `|` 구분선을 넣고 판매 채널 탭 7개(한국단열/한국단열라이프/홈페이지/쿠팡/쿠팡_부자재/ESM/11번가)를 추가했다.

- `HK_CHANNELS` 배열 + `_activeHkChannel` 상태 + `window.setHkChannel(channelId, el)` 추가.
- `initHkPricingTabs()`의 탭바를 `.pricing-tabs-row` 안에 `#hkCategoryTabs`(카테고리)와 `#hkChannelTabs`(채널) 두 그룹으로 나누고 그 사이에 `.hk-tab-divider`를 넣었다 — **두 그룹을 별도 컨테이너로 분리한 이유**: 둘 다 같은 `.pricing-tab` 클래스를 쓰는데, 한 그룹 안에서만 querySelectorAll로 active를 토글해야 카테고리를 바꿔도 채널 선택이 안 풀리고 그 반대도 마찬가지라서(처음엔 같은 `#hkPricingTabsBar` 하나로 스코프해서 만들려다 이 충돌을 미리 발견해서 구조를 나눔).
- **지금은 선택 상태만 있고 실제로 화면 내용을 채널별로 다르게 보여주는 연결은 없다** — 어떤 채널을 고르면 뭐가 달라져야 하는지는 사용자가 아직 얘기 안 함. 필요해지면 `window._activeHkChannel` 값을 참고해서 확장하면 됨.
- 브라우저에서 채널 클릭↔카테고리 클릭이 서로 안 건드리는 것 확인.
- **아직 커밋 안 함.**

## 3단계(채널별 실제 등록 상품) 첫 구현 — "한국단열" 채널 (Claude, 2026-09-16)

채널 탭을 만든 이유가 여기에 3단계 최종 상품데이터를 넣기 위해서였다는 걸 확인. 사용자가 실제 사용하던 엑셀(상품코드로 VLOOKUP해서 만들던 표)을 그대로 반영했다.

- `HK_ISO_CHANNEL_LISTINGS`(채널ID별 배열) 추가 — 지금은 `hkd`(한국단열)에 상품번호 439103571 / 2229818356 두 블록, 각 12개 옵션(일반 8 + 접착식 4)이 들어있다.
- `_hkIsoLookupFinalPriceByCode(code)` — 상품코드로 2단계(`HK_ISO_SHIPPING_BLOCKS`)에서 실판매가를 찾아온다(엑셀 VLOOKUP과 같은 방식). 1단계 DOM의 지금 값을 읽으므로 1→2→3단계가 전부 실시간으로 이어진다.
- 계산 규칙(엑셀 24행 전부 역산해서 확인):
  - 네이버판매가(기본가) = 상품코드로 찾은 2단계 실판매가
  - 기준가 = 같은 상품ID 안에서 제일 싼 네이버판매가
  - 옵션추가금 = 네이버판매가 − 기준가
  - 가격차이 = 네이버판매가(새로 계산한 값) − 수정 전 판매가(그 전에 실제 스토어에 있던 값, 사람이 확인해서 넣어둔 값)
- `_hkIsoProductNameFromCode(code)` — 상품코드 자체(`Iso_430_430_10_3` 등)에 규격·두께·수량이 다 들어있어서 상품명을 따로 안 적고 코드에서 만들어낸다.
- 채널 탭을 클릭하면 `setHkChannel()`이 `#hkChannelListingSection`을 그 채널 것으로 다시 그린다. 자료 없는 채널(한국단열라이프/홈페이지/쿠팡 등)은 "아직 입력 전" 안내만 뜬다.
- **부수적으로 발견해서 같이 고친 것**: 이 3단계 예시에 접착식 600x900 옵션(`IsoA_600_900_*`)이 있었는데, 2단계 쪽에 접착식 600x900용 배송비 블록이 아직 없어서 조회가 실패하고 있었다 — `HK_ISO_SHIPPING_BLOCKS`에 `adhesive_600x900_1` 블록을 새로 추가해서 해결(10T/30T/40T는 원본 엑셀 실측값, 20T는 배송비차액이 정확히 0이 되는 값, 50T는 이번 3단계 예시엔 안 쓰이지만 원본 엑셀에 있던 값 그대로 채워둠). 단, 접착식 600x900의 "묶음"행(10_10, 20_5, 30_3, 40_2, 50_2)은 아직 실측 배송비 자료가 없어서 기준배송비 그대로(plusAmount:0) 임시로 채워뒀다 — 나중에 실측값 받으면 교체 필요.
- 브라우저에서 검증: 24행 전부(네이버판매가/기준가/옵션추가금/가격차이) 엑셀 원본과 정확히 일치, 다른 채널은 정상적으로 "입력 전" 안내가 뜨는 것 확인.
- **아직 커밋 안 함.**

## 카테고리 탭 ↔ 채널 탭 화면 분리 (Claude, 2026-09-16)

사용자 요청: "한국단열 등 채널 탭에서는 기존 아이소핑크·스티로폼 같은 기본 가격 데이터가 안 나오게" — 카테고리 탭과 채널 탭을 같은 화면에 다 보여주던 걸, 둘 중 하나만 보이는 모드 전환으로 바꿨다.

- 1·2단계(공통 원가 입력 + 결과 카드 + 2단계 배송비 섹션)를 `#hkIsoBaseDataSection`으로 통째로 감쌌다.
- `setHkPricingTab()`(카테고리 탭 클릭) → `#hkIsoBaseDataSection` 보이고 `#hkChannelListingSection`(3단계) 숨김 = "카테고리 모드".
- `setHkChannel()`(채널 탭 클릭) → 반대로 `#hkIsoBaseDataSection` 숨기고 `#hkChannelListingSection` 보이고 새로고침 = "채널 모드".
- 기본 진입 상태는 카테고리 모드(아이소핑크, 기존과 동일) — `resetHkPricingView()`가 `setHkPricingTab()`을 호출하므로 자동으로 유지됨.
- 브라우저에서 검증: 채널 클릭 시 base=숨김/listing=24행 보임, 다시 카테고리 클릭 시 base=보임/listing=숨김으로 정확히 전환되는 것 확인.
- **아직 커밋 안 함.**

## 겹침 버그 2건 수정 (Claude, 2026-09-16)

사용자가 "겹치는 등 문제 해결해죠"라고 해서 실제로 화면을 스크린샷으로 확인해서 찾아냈다(이전엔 값만 JS로 대조했지 눈으로 직접 본 적이 없었음 — 그래서 놓쳤던 버그).

1. ~~카테고리 탭 줄 ↔ 채널 탭 줄이 붙어 보이는 문제~~ — **되돌림(아래 참고)**: 사용자가 이 부분은 문제 제기한 적 없다고 해서 원래대로(한 줄 + 세로 구분선) 복원함.
2. **3단계 표의 "상품명"·"상품코드" 글자가 서로 겹치는 문제(진짜 버그)**: `.hk-iso-listing-table`에 `<colgroup>`(열 너비 지정)이 없는 상태로 `table-layout: fixed`(이 앱 표 전체에 적용된 규칙)를 쓰다 보니, 14개 칼럼 너비가 내용과 상관없이 균등 분배돼서 "아이소핑크 430x430 10T_3장"처럼 긴 상품명이 좁은 칸에 눌려 옆 칸(상품코드) 글자와 겹쳐 보였다. `<colgroup>`으로 칼럼별 너비를 직접 지정해서(상품명 220px, 상품코드 170px 등) 해결.
- 브라우저 스크린샷으로 직접 재확인: 3단계 표 글자 안 겹침 정상.
- **아직 커밋 안 함.**

## 되돌림 + 진짜 원인 수정: 카테고리/채널 탭 동시 활성화 (Claude, 2026-09-16)

사용자 피드백: "이건 그전으로 돌려 난 이거에 대해 문제 제기 한적 없음. 그리고 제품카테고리 버튼이랑 채널별 버튼이랑 같이 중복으로 눌려 — 각각 독립된 버튼이니 중복으로 눌릴 이유가 없음."

- 탭 줄 레이아웃(위/아래 분리 + 가로 구분선)은 요청한 적 없는 변경이라 **원래대로(한 줄 + 세로 `|` 구분선) 되돌렸다.**
- 진짜 문제는 따로 있었다: 카테고리 탭 7개 중 첫 번째("아이소핑크")와 채널 탭 7개 중 첫 번째("한국단열")가 **처음부터 둘 다 active로 표시**돼 있었고, `setHkPricingTab()`/`setHkChannel()`이 서로 반대쪽 줄의 active 표시는 전혀 안 지웠다 — 그래서 카테고리를 아무리 바꿔도 채널 쪽 버튼 하나가 계속 파랗게 눌려 있고, 채널을 바꿔도 카테고리 쪽 버튼이 계속 눌려 있는 것처럼 보였다(실제로 지금 보고 있는 화면 모드와 안 맞는 버튼이 같이 강조돼 있던 것).
- 고침: 채널 탭은 처음에 아무것도 active로 시작하지 않게 하고, `setHkPricingTab()`은 채널 탭 쪽 active를 전부 지우고, `setHkChannel()`은 카테고리 탭 쪽 active를 전부 지우도록 해서 — **항상 정확히 하나(지금 보고 있는 화면에 해당하는 탭)만 active로 표시**되게 했다.
- 브라우저에서 검증: 초기 상태(카테고리만 active), 채널 클릭 시(채널만 active·카테고리 active 해제), 다시 카테고리 클릭 시(카테고리만 active·채널 active 해제) 전부 정상.
- **아직 커밋 안 함.**

## 3단계 표 칼럼 너비 조정 (Claude, 2026-09-16)

사용자 요청: 상품명이 더 길어질 수 있으니 다른 칼럼을 줄이고 상품명을 늘리고, 상품코드는 더 줄여도 되고, 우측 끝 "수정 전 판매가/가격차이/수정전 배송비" 3칸은 같은 폭으로.

`.hk-iso-listing-table`의 `<colgroup>` 조정: 상품명 220→300px, 상품코드 170→130px(길어지면 2줄로 자동 줄바꿈 — 이미 `word-break:break-all` 걸어둠), 마지막 3칸(수정 전 판매가/가격차이/수정전 배송비)을 전부 100px로 통일. 표 `min-width`도 1600px로 맞춤. 브라우저에서 칼럼 실제 폭 확인 완료.
- **아직 커밋 안 함.**

## 3단계 표 — 상품ID를 rowspan 병합 셀로 (Claude, 2026-09-16)

사용자 피드백: 상품ID가 "같은 상품"이라는 뜻인데 지금은(같은 상품의 첫 행에만 값 넣고 나머지는 빈칸) 그게 표현이 안 된다 — 빈칸으로 놔두면 엑셀의 병합 셀처럼 안 보이고 그냥 데이터가 빠진 것처럼 보임.

- 진짜 `<td rowspan="N">`으로 상품ID 셀을 그 상품의 옵션 행 수만큼 세로로 병합했다(엑셀에서 하던 셀 병합과 정확히 같은 방식) — 첫 행에만 셀을 만들고 나머지 행은 그 칼럼 자체를 생략(HTML rowspan 규칙).
- 추가로 상품(그룹)별로 행 배경을 흰색/연한 인디고로 번갈아 칠해서(`.hk-iso-listing-group-a`/`-b`) 병합 셀이 없어도 그룹 경계가 한눈에 보이게 했다 — 기본 표의 짝수행 줄무늬보다 우선하도록 선택자를 더 구체적으로 잡음.
- 상품ID 칼럼 자체도 인디고 배경 + 좌우 테두리로 강조해서 "이 칸이 여러 행에 걸쳐있다"가 시각적으로 분명하게 보이게 함.
- 브라우저에서 확인: 상품ID 셀 2개가 각각 정확히 12개 행(그 상품의 옵션 수)만큼 rowspan 됨, 스크린샷으로 그룹 경계도 뚜렷하게 보이는 것 확인.
- **아직 커밋 안 함.**

## 실사용 중 발견된 진짜 버그 + 마무리 다듬기 (Claude, 2026-09-16)

사용자가 실제 앱 화면 스크린샷을 보내서 "439103571랑 2229818356 경계가 없다"고 지적 — 확인해보니 두 가지가 겹쳐서 일어난 문제였다.

**진짜 버그**: `.hk-iso-listing-table`이 `.hk-iso-draft-table` 클래스를 같이 쓰는데, 그 공용 클래스에 `nth-child(3)`/`nth-child(6)`/`nth-child(15)`/`nth-child(16)` 위치 기준 스타일 규칙이 걸려있었다(원래 16칸짜리 기준단가/배송비 표의 특정 칼럼을 겨냥한 것). 그런데 3단계 표는 상품ID 칸에 rowspan을 쓰기 때문에, 같은 상품의 2번째 행부터는 그 칸 자체가 없어서 **뒤 칸들의 nth-child 순번이 전부 한 칸씩 밀렸다** — 그 결과 "네이버판매가"·"재고수량" 같은 엉뚱한 칸에 원래 다른 칼럼(판매사이즈 등)을 위한 파란 글씨/배경이 덧씌워져서 표가 지저분해 보였고, 그 시각적 잡음 때문에 상품ID 그룹 경계(연한 배경 줄무늬만 있던 상태)가 더 안 보였다. `:not(.hk-iso-listing-table)`로 3단계 표를 그 규칙들에서 빼서 해결.
- 그룹 경계는 배경 줄무늬 대신 새 상품 시작 행에 굵은 보라색 윗선(`hk-iso-listing-group-start`)을 긋는 방식으로 바꿨다 — 사용자가 "이렇게 칸 나누면 될 것 같고 배경색 구분은 안 해도 될 것 같다"고 확인해줘서, 배경 줄무늬 코드는 다시 지웠다(굵은 윗선만 남김).
- 표 안 모든 칼럼을 가운데 정렬로 통일해달라는 요청도 반영(원래 상품명만 왼쪽 정렬이었음, 이 표에서만 가운데로 되돌림 — 다른 1·2단계 표는 그대로 왼쪽 유지).
- 브라우저에서 재검증: nth-child 오염 사라짐(재고수량 칸이 다시 기본 스타일로), 상품ID 경계에 굵은 선 정확히 표시, 상품명까지 전부 가운데 정렬 확인.
- **아직 커밋 안 함.**

## 상품ID → 스마트스토어 링크 (Claude, 2026-09-16)

3단계 표의 상품ID(rowspan 병합 셀)를 클릭하면 `https://smartstore.naver.com/hkdy/products/{상품번호}`로 새 탭에서 열리게 했다(`target="_blank" rel="noopener noreferrer"`). 인디고 밑줄 링크 스타일로 표시. 브라우저에서 두 상품ID(439103571, 2229818356) 모두 정확한 URL로 링크가 걸리는 것 확인.
- **아직 커밋 안 함.**

## 접착식 600x900 2단계 데이터 재확인 (Claude, 2026-09-16, 변경 없음)

사용자가 "접착식 압출법 특호 35kg--600*900"(상품번호 439103571 // 2229818356 // 3020442618) 2단계 원본 엑셀 5행을 다시 보내줘서 대조했다 — `adhesive_600x900_1` 블록(지난번에 원본 엑셀에서 직접 읽어 넣어둔 값)과 5행 전부(10T~50T의 첫 변형 행: 실제배송비/배송비차액/1장당차액/배송비플러스금액/실판매가) **정확히 일치**해서 코드 변경 없음. 브라우저에서 재검증 완료.

## 상품번호 439904706 추가 (Claude, 2026-09-16)

사용자가 상품번호 439904706의 "압출법 특호"·"접착식 압출법 특호" 2단계 표를 줬는데, 값을 대조해보니 **기존 `iso_600x900_1`·`adhesive_600x900_1` 블록의 데이터와 전부 정확히 일치**했다(같은 배송비 정책을 쓰는 상품이라는 뜻). 그래서 새 블록을 따로 안 만들고 두 블록의 `productNumbers` 배열에 `'439904706'`만 추가했다 — 데이터 중복 없이 "이 상품번호도 같은 기준단가/배송비를 쓴다"는 걸 표현하는 원래 설계(배열로 여러 상품번호를 한 블록에 묶는 방식) 그대로 따른 것. 3단계(실제 등록 상품) 자료는 이 상품번호에 대해 아직 안 받아서 손대지 않음.

## 900x1800 계열 2단계 — 배송비 구조가 아예 다른 케이스 추가 (Claude, 2026-09-16)

사용자가 "압출법 특호 35kg--900*1800" 2단계 데이터 2블록(유료배송 기본 + 상품번호 5695312387 무료배송)을 줬다. 값 대조 결과, 사이즈/수량 패턴(코드 뒤 숫자: 10_1,20_1... vs 10_10,20_5...)으로 첫 블록은 `iso_900x1800_single`, 둘째 블록은 `iso_900x1800_bundle`(묶음)에 대응한다는 걸 확인(1단계 판매가와도 정확히 일치).

**600x900류와 근본적으로 다른 점 2가지**:
1. 열 구성 자체가 다르다 — "5장당" 기준이 아니라 "1장당 배송비 + 몇 개마다 배송비를 청구하는지(실제배송비가 숫자가 아니라 "5개마다" 같은 텍스트 규칙)" + "알림 쿠폰" 구조.
2. **실판매가 공식이 무료배송 여부에 따라 완전히 다르다**: 유료배송(첫 블록)은 `실판매가 = 판매가` 그대로(배송비 플러스 금액은 참고용, 실제로는 결제 시 배송비를 별도로 받아서 안 얹음) — 반면 무료배송(둘째 블록, 상품번호 "(무료)")은 `실판매가 = 판매가 + 배송비 플러스 금액`(무료배송으로 보이게 배송비를 가격에 얹음). 이건 세션 초반 원본 엑셀을 처음 봤을 때 발견했던 바로 그 문제 — 이번에 처음으로 실제 구현.

**구현**:
- 블록에 `schema` 필드 추가(`'per5'` = 기존 600x900류, `'per1Coupon'` = 이번 900x1800류) + `isFreeShipping` 플래그.
- 새 렌더러 `_hkIsoShippingBlockHtmlPerUnit()` / 재계산 `recalcHkIsoShippingRowPerUnit()` — 무료배송이면 배송비 칸에 "배송비 무료" 라벨만 뜨고 실제배송비는 숫자 입력칸, 유료배송이면 1장당배송비 입력칸 + 실제배송비는 텍스트 입력칸(regex 몇개마다).
- `_hkIsoLookupFinalPriceByCode()`(3단계 VLOOKUP)도 schema별로 플러스금액 적용 여부를 다르게 계산하도록 수정.
- **상품코드 오타 그대로 보존**: 70T·100T 무료배송 옵션의 실제 등록 코드가 `IIso_900_1800_70_1`(I가 두 번)인 걸 원본 엑셀에서도 확인했었는데, 이번에도 똑같이 와서 그대로(고치지 않고) 반영 — `codeOverride` 필드로 자동 생성 코드 대신 이 오타 코드를 쓰게 했다. VLOOKUP 함수도 이 오타 코드로 정확히 찾아지는 것까지 확인.
- 브라우저에서 14행(단품 7 + 묶음 7) 전부 실판매가 검증 — 엑셀과 정확히 일치(단품은 판매가 그대로, 묶음은 판매가+플러스금액).
- **아직 커밋 안 함.**

## 상품ID 중앙정렬 진짜 원인 + 재고수량 하드코딩 제거 (Claude, 2026-09-16)

**중앙정렬 재확인**: 사용자가 "상품ID 중앙정렬 안 되어 있다"고 지적해서 `getComputedStyle`로 직접 찍어봤더니 실제로 `text-align: right`였다. 원인은 `.pricing-table tbody td { text-align: right }`(기본 표 공통 규칙, 클래스 1개+타입 2개)가 `.hk-iso-draft-table td { text-align: center }`(클래스 1개+타입 1개)보다 명시도가 높아서 항상 이기고 있었던 것 — 지난번에 상품명만 센터로 보였던 건 그 선택자가 클래스 2개(`.hk-iso-listing-table .hk-iso-draft-name`)라 우연히 명시도가 더 높아서 먹혔던 거였고, 나머지 칸(상품ID 포함)은 계속 오른쪽 정렬 상태였다. `.hk-iso-listing-table.pricing-table tbody td { text-align:center }`(클래스 2개+타입 2개)로 확실히 이기도록 고쳐서 표 전체 14칸 전부 `getComputedStyle` 기준 `center`로 확인.

**재고수량 하드코딩 제거**: 사용자가 "내부 숫자가 그냥 보여주기용인지 실제 데이터인지 확인해달라"고 해서 표 전체를 다시 훑었다 — "재고수량" 칸만 `<td>99,999,999</td>`로 템플릿에 문자열 그대로 박혀있고 `HK_ISO_CHANNEL_LISTINGS` 데이터와 전혀 연결이 안 돼 있었다(진짜 "보여주기용 숫자"였음). `item.stock ?? 99999999`로 바꿔서 실제 데이터 필드가 될 수 있게 했다 — 지금 예시는 전부 재고 사실상 무제한이라 기본값 그대로 쓰지만, 앞으로 특정 옵션이 품절/한정수량이면 그 항목에 `stock: N`만 추가하면 반영됨. 나머지 칸(배송비/배송비기준/제주배송비/편도교환/수정전판매가/수정전배송비)은 전부 `product`/`item` 데이터에서 오는 걸 재확인 — 하드코딩된 건 재고수량 하나뿐이었음.
- **아직 커밋 안 함.**

## 900x1800/600x900 고티 2단계 나머지 전부 반영 (Claude, 2026-09-16)

사용자가 "알림쿠폰이랑 배송비 플러스 사이 빈 열은 그냥 빈 걸로 두고 채워라"고 해서, per1Coupon 스키마 표에 "1장당배송비차액"(원본 엑셀에도 항상 빈칸) 칼럼을 추가로 넣었다(헤더+`—` 빈 셀). 그 직후 사용자가 남은 2단계 원본 엑셀 데이터를 통째로 줬다(9개 표). 하나씩 값 대조해서 기존 블록 확장 3건 + 신규 블록 4건으로 정리했다.

**기존 블록에 추가만 한 것(데이터가 이미 있던 것과 완전히 일치)**:
- `iso_600x900_1` productNumbers에 `442086644` 추가.
- `adhesive_600x900_1` productNumbers에 `3736232926` 추가, 그리고 이전에 "실측 전이라 자리만 채워둔" 묶음(2번째) 행 5개(10T-10장/20T-5장/30T-3장/40T-2장/50T-2장)를 "쿠팡전용" 표의 실측값으로 교체 — 이 5행은 스마트스토어 상품번호가 아니라 별도 채널("쿠팡전용", 상품번호 없이 채널명으로만 구분)이 쓴다는 것도 주석으로 남겨둠.

**공통 원가/기준단가 구조 일반화**: 600 계열 고티 표가 "5장당"이 아니라 "배송비 무료(0원 기준) + 1장 단위" 구조라 기존 `per5` 렌더러/재계산 함수에 `divisor`(기본 5) 옵션을 추가해서 재사용했다(하드코딩된 ÷5를 ÷divisor로 바꿈, divisor===1이면 헤더도 "배송비 무료"/"실제배송비"로 자동 전환).

**신규 블록 4개**(전부 schema:'per1Coupon', 유료/무료배송 구분 반영):
- `adhesive_900x1800_single_1` — 접착식 900x1800 단품, 유료배송(상품번호 없음, 기본값).
- `adhesive_900x1800_bundle_1` — 접착식 900x1800 묶음, 무료배송(상품번호 5695312387).
- `iso_900x1800_high_t_1` — 900x1800 고티, 무료배송(상품번호 5697937041).
- `iso_600x900_high_t_1` — 600 계열 고티, 무료배송(상품번호 4995022274) — `divisor:1`, `baseShipping5:0`으로 일반화된 per5 렌더러 사용, 배송비 플러스 금액이 원본에 전부 빈 칸이라 0으로 둠(무료배송인데 아직 가격에 안 얹은 상태로 보임).

이걸로 아이소핑크 카테고리의 사이즈/판매방식 조합 9개 중 8개(쿠팡 위너 전체만 남음)에 2단계 데이터가 다 들어갔다.

브라우저에서 전체 검증: 8개 블록 총 69행 전부 엑셀 원본 실판매가와 정확히 일치(신규 블록 포함, divisor=1 일반화 케이스 포함). "1장당배송비차액" 빈 칸 표시도 정상, 상품번호 없는 블록은 "상품번호 미지정(기본값)" 안내 정상 표시.
- **아직 커밋 안 함.**

## 쿠팡 위너 2단계 — 마지막 남은 조합까지 전부 완료 (Claude, 2026-09-16)

사용자가 "쿠팡 위너 전용" 표(26행, 상품코드 접두어 `IsoC_`)를 줬다. 이걸로 아이소핑크 9개 사이즈/판매방식 조합 전부에 2단계가 들어갔다. 이번 건은 지금까지 중 가장 예외가 많아서 `per5` 렌더러/계산 함수를 두 번 더 일반화했다.

1. **상품코드 접두어 확장**: `_hkIsoDraftProductCode()`에 `codePrefix` 인자를 추가해서 `Iso_`/`IsoA_` 외에 `IsoC_`(쿠팡 위너)도 만들 수 있게 함 — `_hkIsoLookupFinalPriceByCode()`도 같이 반영.
2. **행마다 기준배송비가 다름**: 이 표는 수량이 많은 행(10장/20장)일수록 기준배송비도 배수로 올라간다(6000원 기준 × 묶음수, 예: 10장→12000, 20장→24000). 블록 전체에 고정값 하나만 쓰던 `baseShipping5`를 행별로 덮어쓸 수 있게(`ship.baseShipping`) 확장.
3. **"5장당" 개념이 아예 없는 행**: 600*860/600*430/900*1800 규격(큰 두께)은 원본 엑셀에도 기준배송비·배송비차액 칸이 통째로 비어있다 — `baseShipping: null`이면 그 계산 자체를 건너뛰고 화면에도 "—"만 표시하도록 `hasBase` 분기를 추가(재계산 함수도 같이 대응).
4. **"1장당배송비차액"이 수량마다 다르게 나뉨**: 대부분 행은 배송비차액을 그 행의 실제 판매수량으로 나누면 원본 값과 맞아떨어져서(`perRowDivisor:true`, 판매사이즈 뒤 숫자로 나눔) 그렇게 계산했다. 단, 40T-5장·50T-2장 두 행은 원본 엑셀 자체에서 이 계산과 안 맞는 값이 적혀있었다(같은 수량인 다른 행들과 비교해도 일관성이 없어 원본 입력 실수로 보임) — 이 칸은 참고용 표시일 뿐 실판매가 계산에는 전혀 쓰이지 않으므로, 정확한 실판매가를 지키는 쪽을 택하고 이 두 칸만 원본과 다르게 보일 수 있음을 주석으로 남겼다.

브라우저에서 26행 전부(상품코드 포함) 검증 — 실판매가 전부 정확히 일치, "5장당" 개념이 없는 행은 기준배송비/배송비차액/1장당차액 칸에 "—"가 제대로 뜨고 실제배송비·플러스금액·실판매가는 정상 표시되는 것 확인.

이걸로 **한국단열 아이소핑크 2단계(배송비/실판매가) 작업이 사용자가 준 원본 엑셀 기준으로 전부 끝났다** — 9개 조합(일반 600x900/600 계열 고티/900x1800 단품·묶음·고티, 접착식 600x900/900x1800 단품·묶음, 쿠팡 위너) 모두 반영됨. 3단계(채널별 실제 등록 상품)는 "한국단열" 채널의 상품번호 439103571/2229818356만 들어가 있고 나머지 상품번호·채널은 아직임.
- **아직 커밋 안 함.**

