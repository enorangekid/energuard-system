# 통합관리자 스포츠 위젯 보관본

이 폴더는 Orange Sports 웹 전환 과정에서 비교하기 위한 레거시 코드 보관소다.

- `widget-sports.js`: 과거 에너가드 통합관리자에서 실행되던 스포츠 패널 코드
- 현재 통합관리자 `index.html`에서는 이 파일을 로드하지 않는다.
- 필요한 로직을 Orange Sports 웹으로 이식한 뒤 이 폴더 전체를 삭제할 수 있다.
- 당시 `.sp-*` 스타일은 통합관리자 `css/panels.css`와 `css/style.css`에 남아 있다.
